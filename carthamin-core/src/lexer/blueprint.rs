// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: BlueprintLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated BlueprintLexer lexer.
pub struct BlueprintLexer {
    inner: RegexLexer,
}

impl BlueprintLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Blueprint");
        inner.aliases = vec![
            "blueprint",
        ];
        inner.filenames = vec![
            "*.blp",
        ];
        inner.mimetypes = vec![
            "text/x-blueprint",
        ];

        // State: type
        inner.states.insert("type".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\$\s*[a-z_][a-z0-9_\-]*", Token::NAME_CLASS).unwrap(), action: LexerAction::token(Token::NAME_CLASS) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"//.*?\n", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"/\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("comment-multiline".to_string()) },
        ]);

        // State: comment-multiline
        inner.states.insert("comment-multiline".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\*/", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^*]+", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(false|null|true)", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"[a-z_][a-z0-9_\-]*", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r"\|", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r##"".*?""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
            LexerRule { pattern: TokenPattern::new(r"\'.*?\'", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"0x[\d_]*", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"[0-9_]+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
            LexerRule { pattern: TokenPattern::new(r"\d[\d\.a-z_]*", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
        ]);

        // State: typeof
        inner.states.insert("typeof".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r">", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: content
        inner.states.insert("content".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(after|bi(?:directional|nd(?:(?:\-property)?))|d(?:e(?:fault|structive)|isabled)|inverted|no\-sync\-create|s(?:uggested|wapped|ync\-create)|template)", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r",|\.", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: block-content
        inner.states.insert("block-content".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"template", Token::KEYWORD_DECLARATION).unwrap(), action: LexerAction::Push("template".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\[", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("child-type".to_string()) },
        ]);

        // State: paren-block
        inner.states.insert("paren-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: paren-content
        inner.states.insert("paren-content".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: bracket-block
        inner.states.insert("bracket-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\]", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: brace-block
        inner.states.insert("brace-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: statement
        inner.states.insert("statement".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: child-type
        inner.states.insert("child-type".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(default|internal\-child|response)", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"[a-z_][a-z0-9_\-]*", Token::NAME_DECORATOR).unwrap(), action: LexerAction::token(Token::NAME_DECORATOR) },
            LexerRule { pattern: TokenPattern::new(r"=", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\]", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: template
        inner.states.insert("template".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r":", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        BlueprintLexer { inner }
    }
}

impl Lexer for BlueprintLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
