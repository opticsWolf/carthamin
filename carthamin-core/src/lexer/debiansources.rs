// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: DebianSourcesLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated DebianSourcesLexer lexer.
pub struct DebianSourcesLexer {
    inner: RegexLexer,
}

impl DebianSourcesLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Debian Sources file");
        inner.aliases = vec![
            "debian.sources",
        ];
        inner.filenames = vec![
            "*.sources",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: signed-by
        inner.states.insert("signed-by".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r" -----END PGP PUBLIC KEY BLOCK-----\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r".+\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        DebianSourcesLexer { inner }
    }
}

impl Lexer for DebianSourcesLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
