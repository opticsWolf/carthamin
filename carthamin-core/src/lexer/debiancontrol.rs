// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: DebianControlLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated DebianControlLexer lexer.
pub struct DebianControlLexer {
    inner: RegexLexer,
}

impl DebianControlLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Debian Control file");
        inner.aliases = vec![
            "debcontrol",
            "control",
        ];
        inner.filenames = vec![
            "control",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(Description)", Token::KEYWORD).unwrap(), action: LexerAction::Push("description".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: maintainer
        inner.states.insert("maintainer".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<[^>]+>$", Token::GENERIC_STRONG).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"<[^>]+>", Token::GENERIC_STRONG).unwrap(), action: LexerAction::token(Token::GENERIC_STRONG) },
            LexerRule { pattern: TokenPattern::new(r"[^,<]+$", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^,<]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: description
        inner.states.insert("description".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r":.*\n", Token::GENERIC_STRONG).unwrap(), action: LexerAction::token(Token::GENERIC_STRONG) },
            LexerRule { pattern: TokenPattern::new(r" .*\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: package_list
        inner.states.insert("package_list".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\(", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("package_list_vers".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\|", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"[,\s]", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"[+.a-zA-Z0-9-]+", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
        ]);

        // State: package_list_vers
        inner.states.insert("package_list_vers".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        DebianControlLexer { inner }
    }
}

impl Lexer for DebianControlLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
