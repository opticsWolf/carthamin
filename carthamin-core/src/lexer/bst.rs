// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: BSTLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated BSTLexer lexer.
pub struct BSTLexer {
    inner: RegexLexer,
}

impl BSTLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("BST");
        inner.aliases = vec![
            "bst",
            "bst-pybtex",
        ];
        inner.filenames = vec![
            "*.bst",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(read|sort)", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(execute|i(?:ntegers|terate)|reverse|strings)", Token::KEYWORD).unwrap(), action: LexerAction::Push("group".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(function|macro)", Token::KEYWORD).unwrap(), action: LexerAction::Push("group".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(entry)", Token::KEYWORD).unwrap(), action: LexerAction::Push("group".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: group
        inner.states.insert("group".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: group-end
        inner.states.insert("group-end".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: body
        inner.states.insert("body".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"\'[^#\"\{\}\s]+"#, Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
            LexerRule { pattern: TokenPattern::new(r#"[^#\"\{\}\s]+\$"#, Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
            LexerRule { pattern: TokenPattern::new(r#"[^#\"\{\}\s]+"#, Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r##""[^\"]*""##, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"#-?\d+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("group-end".to_string()) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%.*?$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
        ]);

        BSTLexer { inner }
    }
}

impl Lexer for BSTLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
