// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: PythonTracebackLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated PythonTracebackLexer lexer.
pub struct PythonTracebackLexer {
    inner: RegexLexer,
}

impl PythonTracebackLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Python Traceback");
        inner.aliases = vec![
            "pytb",
            "py3tb",
        ];
        inner.filenames = vec![
            "*.pytb",
            "*.py3tb",
        ];
        inner.mimetypes = vec![
            "text/x-python-traceback",
            "text/x-python3-traceback",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(\^C)?Traceback \(most recent call last\):\n", Token::GENERIC_TRACEBACK).unwrap(), action: LexerAction::Push("intb".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^During handling of the above exception, another exception occurred:\n\n", Token::GENERIC_TRACEBACK).unwrap(), action: LexerAction::token(Token::GENERIC_TRACEBACK) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^The above exception was the direct cause of the following exception:\n\n", Token::GENERIC_TRACEBACK).unwrap(), action: LexerAction::token(Token::GENERIC_TRACEBACK) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"^(?=  File "[^"]+", line \d+)"#, Token::GENERIC_TRACEBACK).unwrap(), action: LexerAction::Push("intb".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^.*\n", Token::OTHER).unwrap(), action: LexerAction::token(Token::OTHER) });

        inner.states.insert("root".to_string(), root_rules);

        // State: intb
        inner.states.insert("intb".to_string(), vec![
        ]);

        // State: markers
        inner.states.insert("markers".to_string(), vec![
        ]);

        PythonTracebackLexer { inner }
    }
}

impl Lexer for PythonTracebackLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
