// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: RstLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated RstLexer lexer.
pub struct RstLexer {
    inner: RegexLexer,
}

impl RstLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("reStructuredText");
        inner.aliases = vec![
            "restructuredtext",
            "rst",
            "rest",
        ];
        inner.filenames = vec![
            "*.rst",
            "*.rest",
        ];
        inner.mimetypes = vec![
            "text/x-rst",
            "text/prs.fallenstein.rst",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^ *\.\..*(\n( +.*\n|\n)+)?", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: inline
        inner.states.insert("inline".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"``", Token::STRING).unwrap(), action: LexerAction::Push("literal".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"`.+?`__?", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\*\*.+?\*\*", Token::GENERIC_STRONG).unwrap(), action: LexerAction::token(Token::GENERIC_STRONG) },
            LexerRule { pattern: TokenPattern::new(r"\*.+?\*", Token::GENERIC_EMPH).unwrap(), action: LexerAction::token(Token::GENERIC_EMPH) },
            LexerRule { pattern: TokenPattern::new(r"\[.*?\]_", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"<.+?>", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) },
            LexerRule { pattern: TokenPattern::new(r"[^\\\n\[*`:]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r".", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: literal
        inner.states.insert("literal".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^`]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r#"``((?=$)|(?=[-/:.,; \n\x00‐‑‒–— '"\)\]\}>’”»!\?]))"#, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"`", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        RstLexer { inner }
    }
}

impl Lexer for RstLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
