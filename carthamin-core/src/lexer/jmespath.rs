// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: JMESPathLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated JMESPathLexer lexer.
pub struct JMESPathLexer {
    inner: RegexLexer,
}

impl JMESPathLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("JMESPath");
        inner.aliases = vec![
            "jmespath",
            "jp",
        ];
        inner.filenames = vec![
            "*.jp",
        ];

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"'(\\(.|\n)|[^'\\])*'", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: punctuation
        inner.states.insert("punctuation".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(\[\?|[\.\*\[\],:\(\)\{\}\|])", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: ws
        inner.states.insert("ws".to_string(), vec![
        ]);

        // State: dq-identifier
        inner.states.insert("dq-identifier".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"[^\\"]+"#, Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r##"\\""##, Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r".", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: identifier
        inner.states.insert("identifier".to_string(), vec![
        ]);

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(==|!=|<=|>=|<|>|&&|\|\||!)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"@", Token::NAME_VARIABLE_GLOBAL).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE_GLOBAL) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"-?\d+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"`", Token::LITERAL).unwrap(), action: LexerAction::Push("literal".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: literal
        inner.states.insert("literal".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(false|true|null)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"-?\d+\.?\d*([eE][-+]\d+)?", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"\\`", Token::LITERAL).unwrap(), action: LexerAction::token(Token::LITERAL) },
            LexerRule { pattern: TokenPattern::new(r"`", Token::LITERAL).unwrap(), action: LexerAction::PopN(1) },
        ]);

        JMESPathLexer { inner }
    }
}

impl Lexer for JMESPathLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
