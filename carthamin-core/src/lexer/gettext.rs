// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: GettextLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated GettextLexer lexer.
pub struct GettextLexer {
    inner: RegexLexer,
}

impl GettextLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Gettext Catalog");
        inner.aliases = vec![
            "pot",
            "po",
        ];
        inner.filenames = vec![
            "*.pot",
            "*.po",
        ];
        inner.mimetypes = vec![
            "application/x-gettext",
            "text/x-gettext",
            "text/gettext",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^#,\s.*?$", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^#:\s.*?$", Token::KEYWORD_DECLARATION).unwrap(), action: LexerAction::token(Token::KEYWORD_DECLARATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(#|#\.\s|#\|\s|#~\s|#\s).*$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"^".*"$"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) });

        inner.states.insert("root".to_string(), root_rules);

        GettextLexer { inner }
    }
}

impl Lexer for GettextLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
