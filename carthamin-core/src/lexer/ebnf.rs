// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: EbnfLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated EbnfLexer lexer.
pub struct EbnfLexer {
    inner: RegexLexer,
}

impl EbnfLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("EBNF");
        inner.aliases = vec![
            "ebnf",
        ];
        inner.filenames = vec![
            "*.ebnf",
        ];
        inner.mimetypes = vec![
            "text/x-ebnf",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"=", Token::OPERATOR).unwrap(), action: LexerAction::Push("production".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: production
        inner.states.insert("production".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##""[^"]*""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
            LexerRule { pattern: TokenPattern::new(r"'[^']*'", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"(\?[^?]*\?)", Token::NAME_ENTITY).unwrap(), action: LexerAction::token(Token::NAME_ENTITY) },
            LexerRule { pattern: TokenPattern::new(r"[\[\]{}(),|]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"-", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r";", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\.", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: comment_start
        inner.states.insert("comment_start".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\(\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("comment".to_string()) },
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^*)]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"\*\)", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[*)]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: identifier
        inner.states.insert("identifier".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"([a-zA-Z][\w \-]*)", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        EbnfLexer { inner }
    }
}

impl Lexer for EbnfLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
