// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: CheetahLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated CheetahLexer lexer.
pub struct CheetahLexer {
    inner: RegexLexer,
}

impl CheetahLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Cheetah");
        inner.aliases = vec![
            "cheetah",
            "spitfire",
        ];
        inner.filenames = vec![
            "*.tmpl",
            "*.spt",
        ];
        inner.mimetypes = vec![
            "application/x-cheetah",
            "application/x-spitfire",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#[*](.|\n)*?[*]#", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#end[^#\n]*(?:#|$)", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#slurp$", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?sx)
                (.+?)               # anything, followed by:
                (?:
                 (?=\#[#a-zA-Z]*) | # an eval comment
                 (?=\$[a-zA-Z_{]) | # a substitution
                 \Z                 # end of string
                )
            ", Token::OTHER).unwrap(), action: LexerAction::token(Token::OTHER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        CheetahLexer { inner }
    }
}

impl Lexer for CheetahLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
