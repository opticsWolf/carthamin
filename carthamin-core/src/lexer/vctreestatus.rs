// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: VCTreeStatusLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated VCTreeStatusLexer lexer.
pub struct VCTreeStatusLexer {
    inner: RegexLexer,
}

impl VCTreeStatusLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("VCTreeStatus");
        inner.aliases = vec![
            "vctreestatus",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^A  \+  C\s+", Token::GENERIC_ERROR).unwrap(), action: LexerAction::token(Token::GENERIC_ERROR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^A\s+\+?\s+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^M\s+", Token::GENERIC_INSERTED).unwrap(), action: LexerAction::token(Token::GENERIC_INSERTED) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^C\s+", Token::GENERIC_ERROR).unwrap(), action: LexerAction::token(Token::GENERIC_ERROR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^D\s+", Token::GENERIC_DELETED).unwrap(), action: LexerAction::token(Token::GENERIC_DELETED) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[?!]\s+", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"      >\s+.*\n", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\S+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        VCTreeStatusLexer { inner }
    }
}

impl Lexer for VCTreeStatusLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
