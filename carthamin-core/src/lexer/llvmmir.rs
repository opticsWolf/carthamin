// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: LlvmMirLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated LlvmMirLexer lexer.
pub struct LlvmMirLexer {
    inner: RegexLexer,
}

impl LlvmMirLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("LLVM-MIR");
        inner.aliases = vec![
            "llvm-mir",
        ];
        inner.filenames = vec![
            "*.mir",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#.*", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"--- \|$", Token::KEYWORD).unwrap(), action: LexerAction::Push("llvm_ir".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"---", Token::KEYWORD).unwrap(), action: LexerAction::Push("llvm_mir".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^-#]+|.", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: llvm_ir
        inner.states.insert("llvm_ir".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(\.\.\.|(?=---))", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: llvm_mir
        inner.states.insert("llvm_mir".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#.*", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"(\.\.\.|(?=---))", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"name:", Token::KEYWORD).unwrap(), action: LexerAction::Push("name".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"(alignment):", Token::KEYWORD).unwrap(), action: LexerAction::Push("number".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"(exposesReturnsTwice|legalized|regBankSelected|selected|tracksRegLiveness):", Token::KEYWORD).unwrap(), action: LexerAction::Push("boolean".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"(f(?:ixedStack|rameInfo)|liveins|machineFunctionInfo|registers|stack):", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"body: *\|", Token::KEYWORD).unwrap(), action: LexerAction::Push("llvm_mir_body".to_string()) },
            LexerRule { pattern: TokenPattern::new(r".+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: name
        inner.states.insert("name".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\n]+", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
        ]);

        // State: boolean
        inner.states.insert("boolean".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r" *(true|false)", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: number
        inner.states.insert("number".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r" *[0-9]+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
        ]);

        // State: llvm_mir_body
        inner.states.insert("llvm_mir_body".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(\.\.\.|(?=---))", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
        ]);

        LlvmMirLexer { inner }
    }
}

impl Lexer for LlvmMirLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
