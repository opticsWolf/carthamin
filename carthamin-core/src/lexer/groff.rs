// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: GroffLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated GroffLexer lexer.
pub struct GroffLexer {
    inner: RegexLexer,
}

impl GroffLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Groff");
        inner.aliases = vec![
            "groff",
            "nroff",
            "man",
        ];
        inner.filenames = vec![
            "*.[1-9]",
            "*.man",
            "*.1p",
            "*.3pm",
        ];
        inner.mimetypes = vec![
            "application/x-troff",
            "text/troff",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\.", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("request".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^\\\n]+", Token::TEXT).unwrap(), action: LexerAction::Push("textline".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: textline
        inner.states.insert("textline".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\\\n]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: escapes
        inner.states.insert("escapes".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"\\"[^\n]*"#, Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"\\[fn]\w", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\\(.{2}", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\.\[.*\]", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::TEXT).unwrap(), action: LexerAction::Push("request".to_string()) },
        ]);

        // State: request
        inner.states.insert("request".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r##""[^\n"]+""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
            LexerRule { pattern: TokenPattern::new(r"\d+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"\S+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        GroffLexer { inner }
    }
}

impl Lexer for GroffLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
