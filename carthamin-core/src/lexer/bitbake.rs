// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: BitBakeLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated BitBakeLexer lexer.
pub struct BitBakeLexer {
    inner: RegexLexer,
}

impl BitBakeLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("BitBake");
        inner.aliases = vec![
            "bitbake",
        ];
        inner.filenames = vec![
            "*.bbclass",
            "*.bbappend",
        ];
        inner.mimetypes = vec![
            "text/x-bitbake",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#.*$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(inherit_defer|inherit|include_all|include|require)\b", Token::KEYWORD_NAMESPACE).unwrap(), action: LexerAction::Push("include-line".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(addtask|deltask|addhandler|EXPORT_FUNCTIONS)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("statement".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"[^\s#${}\[\]:=+?.@\\"\']+"#, Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r".", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: include-line
        inner.states.insert("include-line".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"[^\s$]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: statement
        inner.states.insert("statement".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"(after|before)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"[^\s$\\]+", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::Push("string-double".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"'", Token::STRING_SINGLE).unwrap(), action: LexerAction::Push("string-single".to_string()) },
            LexerRule { pattern: TokenPattern::new(r#"[^\s"\'$\\]+"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: string-double
        inner.states.insert("string-double".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r#"[^"\\$]+"#, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
        ]);

        // State: string-single
        inner.states.insert("string-single".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r"'", Token::STRING_SINGLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^'\\$]+", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) },
        ]);

        // State: interp
        inner.states.insert("interp".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\$\{@", Token::STRING_INTERPOL).unwrap(), action: LexerAction::Push("py-interp".to_string()) },
        ]);

        // State: py-interp
        inner.states.insert("py-interp".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::STRING_INTERPOL).unwrap(), action: LexerAction::PopN(1) },
        ]);

        BitBakeLexer { inner }
    }
}

impl Lexer for BitBakeLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
