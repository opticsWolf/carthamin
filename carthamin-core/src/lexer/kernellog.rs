// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: KernelLogLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated KernelLogLexer lexer.
pub struct KernelLogLexer {
    inner: RegexLexer,
}

impl KernelLogLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Kernel log");
        inner.aliases = vec![
            "kmsg",
            "dmesg",
        ];
        inner.filenames = vec![
            "*.kmsg",
            "*.dmesg",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:debug : (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("debug".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:info  : (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("info".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:warn  : (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("warn".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:notice: (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("warn".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:err   : (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("error".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^[^:]+:crit  : (?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("error".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^(?=\[)", Token::TEXT).unwrap(), action: LexerAction::Push("unknown".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: unknown
        inner.states.insert("unknown".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"^(?=.+(warning|notice|audit|deprecated))", Token::TEXT).unwrap(), action: LexerAction::Push("warn".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"^(?=.+(error|critical|fail|Bug))", Token::TEXT).unwrap(), action: LexerAction::Push("error".to_string()) },
        ]);

        // State: base
        inner.states.insert("base".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\[[0-9. ]+\] ", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"(?<=\] ).+?:", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: debug
        inner.states.insert("debug".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+\n", Token::COMMENT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: info
        inner.states.insert("info".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: warn
        inner.states.insert("warn".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+\n", Token::GENERIC_STRONG).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: error
        inner.states.insert("error".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+\n", Token::GENERIC_ERROR).unwrap(), action: LexerAction::PopN(1) },
        ]);

        KernelLogLexer { inner }
    }
}

impl Lexer for KernelLogLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
