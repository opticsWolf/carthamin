// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: NotmuchLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated NotmuchLexer lexer.
pub struct NotmuchLexer {
    inner: RegexLexer,
}

impl NotmuchLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Notmuch");
        inner.aliases = vec![
            "notmuch",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\fmessage\{\s*", Token::KEYWORD).unwrap(), action: LexerAction::Push("message".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: message-attr
        inner.states.insert("message-attr".to_string(), vec![
        ]);

        // State: message
        inner.states.insert("message".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\fmessage\}\n", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\fheader\{\n", Token::KEYWORD).unwrap(), action: LexerAction::Push("header".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\fbody\{\n", Token::KEYWORD).unwrap(), action: LexerAction::Push("body".to_string()) },
        ]);

        // State: header
        inner.states.insert("header".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\fheader\}\n", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: body
        inner.states.insert("body".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\fpart\{\n", Token::KEYWORD).unwrap(), action: LexerAction::Push("part".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\f(part|attachment)\{\s*", Token::KEYWORD).unwrap(), action: LexerAction::Push("part".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\fbody\}\n", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: part-attr
        inner.states.insert("part-attr".to_string(), vec![
        ]);

        // State: part
        inner.states.insert("part".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\f(?:part|attachment)\}\n", Token::KEYWORD).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\f(?:part|attachment)\{\s*", Token::KEYWORD).unwrap(), action: LexerAction::Push("part-attr".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"^Non-text part: .*\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
        ]);

        NotmuchLexer { inner }
    }
}

impl Lexer for NotmuchLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
