// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: TactLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated TactLexer lexer.
pub struct TactLexer {
    inner: RegexLexer,
}

impl TactLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Tact");
        inner.aliases = vec![
            "tact",
        ];
        inner.filenames = vec![
            "*.tact",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[.;(),\[\]{}]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\?|!!", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });

        inner.states.insert("root".to_string(), root_rules);

        // State: import-in
        inner.states.insert("import-in".to_string(), vec![
        ]);

        // State: import
        inner.states.insert("import".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: struct-in
        inner.states.insert("struct-in".to_string(), vec![
        ]);

        // State: struct
        inner.states.insert("struct".to_string(), vec![
        ]);

        // State: struct-header
        inner.states.insert("struct-header".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::NAME_CLASS).unwrap(), action: LexerAction::token(Token::NAME_CLASS) },
        ]);

        // State: struct-body-in
        inner.states.insert("struct-body-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("struct-body".to_string()) },
        ]);

        // State: struct-body
        inner.states.insert("struct-body".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: contract-or-trait-in
        inner.states.insert("contract-or-trait-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"((?<=\.\.\.)|(?<![.$]))\b(contract|trait)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("contract-or-trait".to_string()) },
        ]);

        // State: contract-or-trait
        inner.states.insert("contract-or-trait".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"with", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::NAME_CLASS).unwrap(), action: LexerAction::token(Token::NAME_CLASS) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: contract-or-trait-body-in
        inner.states.insert("contract-or-trait-body-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("contract-or-trait-body".to_string()) },
        ]);

        // State: contract-or-trait-body
        inner.states.insert("contract-or-trait-body".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: field-declaration-in
        inner.states.insert("field-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::NAME_PROPERTY).unwrap(), action: LexerAction::Push("field-declaration".to_string()) },
        ]);

        // State: field-declaration
        inner.states.insert("field-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: const-declaration-in
        inner.states.insert("const-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\b(?:(?:get|native|extends|mutates|virtual|override|inline|abstract)\s*)*const\b)", Token::KEYWORD).unwrap(), action: LexerAction::Push("const-declaration".to_string()) },
        ]);

        // State: const-declaration
        inner.states.insert("const-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(;)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"const", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"(abstract|extends|get|inline|mutates|native|override|virtual)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b\w+\b", Token::NAME_CONSTANT).unwrap(), action: LexerAction::token(Token::NAME_CONSTANT) },
        ]);

        // State: init-declaration-in
        inner.states.insert("init-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(init)", Token::KEYWORD).unwrap(), action: LexerAction::Push("init-declaration".to_string()) },
        ]);

        // State: init-declaration
        inner.states.insert("init-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\})", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: receive-declaration-in
        inner.states.insert("receive-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(receive|exernal)", Token::KEYWORD).unwrap(), action: LexerAction::Push("receive-declaration".to_string()) },
        ]);

        // State: receive-declaration
        inner.states.insert("receive-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\})", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: bounce-declaration-in
        inner.states.insert("bounce-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(bounced)", Token::KEYWORD).unwrap(), action: LexerAction::Push("bounce-declaration".to_string()) },
        ]);

        // State: bounce-declaration
        inner.states.insert("bounce-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\})", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: fun-declaration-in
        inner.states.insert("fun-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\b(?:(?:get|native|extends|mutates|virtual|override|inline|abstract)\s*)*fun\b)", Token::KEYWORD).unwrap(), action: LexerAction::Push("fun-declaration".to_string()) },
        ]);

        // State: fun-declaration
        inner.states.insert("fun-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\}|\;)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"fun", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b(get|native|extends|mutates|virtual|override|inline|abstract)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b[\w]+", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
            LexerRule { pattern: TokenPattern::new(r"[,;]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: fun-declaration-body
        inner.states.insert("fun-declaration-body".to_string(), vec![
        ]);

        // State: fun-arguments-in
        inner.states.insert("fun-arguments-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\(", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("fun-arguments".to_string()) },
        ]);

        // State: fun-arguments
        inner.states.insert("fun-arguments".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: block-declaration-in
        inner.states.insert("block-declaration-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("block-declaration".to_string()) },
        ]);

        // State: block-declaration
        inner.states.insert("block-declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: annotation-in
        inner.states.insert("annotation-in".to_string(), vec![
        ]);

        // State: annotation
        inner.states.insert("annotation".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: annotation-argument
        inner.states.insert("annotation-argument".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\w+", Token::NAME_FUNCTION_MAGIC).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION_MAGIC) },
        ]);

        // State: struct-init-in
        inner.states.insert("struct-init-in".to_string(), vec![
        ]);

        // State: struct-init
        inner.states.insert("struct-init".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(\})", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: struct-property-in
        inner.states.insert("struct-property-in".to_string(), vec![
        ]);

        // State: struct-property
        inner.states.insert("struct-property".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\}|\,)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: variable-init-in
        inner.states.insert("variable-init-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(=)", Token::OPERATOR).unwrap(), action: LexerAction::Push("variable-init".to_string()) },
        ]);

        // State: variable-init
        inner.states.insert("variable-init".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\}|\{|\,|\;)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: type-annotation-in
        inner.states.insert("type-annotation-in".to_string(), vec![
        ]);

        // State: type-annotation
        inner.states.insert("type-annotation".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\{|\;|\=|\,|\))", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\?", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
        ]);

        // State: type-generic-in
        inner.states.insert("type-generic-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("type-generic".to_string()) },
        ]);

        // State: type-generic
        inner.states.insert("type-generic".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r">", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: type-as-in
        inner.states.insert("type-as-in".to_string(), vec![
        ]);

        // State: type-as
        inner.states.insert("type-as".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?=\{|\;|\=|\,|\)|\>)", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\b\w+", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
        ]);

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(as|bounced|const|do|e(?:lse|xtends)|fun|get|i(?:n(?:itOf|line)|[fs])|let|m(?:ap|utates)|native|override|re(?:peat|turn)|self|until|virtual|while)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"(<=>|>=|<=|!=|==|\^>>|~>>|>>|<<|\/%|\^%|~%|\^\/|~\/|\+=|-=|\*=|\/=|~\/=|\^\/=|%=|\^%=|<<=|>>=|~>>=|\^>>=|&=|\|=|\^=|\^|=|~|\/|%|-|\*|\+|>|<|&|\||:|\?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\b((?:fals|tru)e)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
        ]);

        // State: string-in
        inner.states.insert("string-in".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::Push("string".to_string()) },
        ]);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r#"[^\\"]+"#, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
        ]);

        // State: numeric
        inner.states.insert("numeric".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?:\b0[xX])[0-9a-fA-F][0-9a-fA-F_]*\b", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"(?:\b[0-9]+\b)", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
        ]);

        // State: comments
        inner.states.insert("comments".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"//.*", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"/\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("comments-multiline".to_string()) },
        ]);

        // State: comments-multiline
        inner.states.insert("comments-multiline".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\*/", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^*]+", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"[*]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: variable
        inner.states.insert("variable".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b\w+\b(?!\s*\()(?!\s*\{)", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        // State: function-call
        inner.states.insert("function-call".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b\w+\b(?=\s*\()(?!\s*\{)", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
        ]);

        TactLexer { inner }
    }
}

impl Lexer for TactLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
