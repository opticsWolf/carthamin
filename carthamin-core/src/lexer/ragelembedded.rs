// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: RagelEmbeddedLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated RagelEmbeddedLexer lexer.
pub struct RagelEmbeddedLexer {
    inner: RegexLexer,
}

impl RagelEmbeddedLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Embedded Ragel");
        inner.aliases = vec![
            "ragel-em",
        ];
        inner.filenames = vec![
            "*.rl",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"([^%\'"/#]+|%(?=[^%]|$)|"(\\\\|\\[^\\]|[^"\\])*"|'(\\\\|\\[^\\]|[^'\\])*'|/\*(.|\n)*?\*/|//.*$\n?|\#.*$\n?|/(?!\*)(\\\\|\\[^\\]|[^/\\])*/|/)+"#, Token::OTHER).unwrap(), action: LexerAction::token(Token::OTHER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(%%%%|%%)\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("multi-line-fsm".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: multi-line-fsm
        inner.states.insert("multi-line-fsm".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}%%", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        RagelEmbeddedLexer { inner }
    }
}

impl Lexer for RagelEmbeddedLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
