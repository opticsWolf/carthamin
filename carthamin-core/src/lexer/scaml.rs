// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: ScamlLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated ScamlLexer lexer.
pub struct ScamlLexer {
    inner: RegexLexer,
}

impl ScamlLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Scaml");
        inner.aliases = vec![
            "scaml",
        ];
        inner.filenames = vec![
            "*.scaml",
        ];
        inner.mimetypes = vec![
            "text/x-scaml",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[ \t]*\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: css
        inner.states.insert("css".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\.[\w:-]+", Token::NAME_CLASS).unwrap(), action: LexerAction::Push("tag".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\#[\w:-]+", Token::NAME_FUNCTION).unwrap(), action: LexerAction::Push("tag".to_string()) },
        ]);

        // State: eval-or-plain
        inner.states.insert("eval-or-plain".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[&!]?==", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("plain".to_string()) },
        ]);

        // State: content
        inner.states.insert("content".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%[\w:-]+", Token::NAME_TAG).unwrap(), action: LexerAction::Push("tag".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"!!!.*\n", Token::NAME_NAMESPACE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: tag
        inner.states.insert("tag".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\(", Token::TEXT).unwrap(), action: LexerAction::Push("html-attributes".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"/[ \t]*\n", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[<>]{1,2}(?=[ \t=])", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: plain
        inner.states.insert("plain".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"([^#\n]|#[^{\n]|(\\\\)*\\#\{)+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::Push("root".to_string()) },
        ]);

        // State: html-attributes
        inner.states.insert("html-attributes".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"[\w:-]+[ \t]*=", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::Push("html-attribute-value".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[\w:-]+", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
            LexerRule { pattern: TokenPattern::new(r"\)", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: html-attribute-value
        inner.states.insert("html-attribute-value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[ \t]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\w+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"@\w+", Token::NAME_VARIABLE_INSTANCE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\$\w+", Token::NAME_VARIABLE_GLOBAL).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"'(\\\\|\\[^\\]|[^'\\\n])*'", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r##""(\\\\|\\[^\\]|[^"\\\n])*""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: html-comment-block
        inner.states.insert("html-comment-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::Push("root".to_string()) },
        ]);

        // State: scaml-comment-block
        inner.states.insert("scaml-comment-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".+", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::Push("root".to_string()) },
        ]);

        // State: filter-block
        inner.states.insert("filter-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"([^#\n]|#[^{\n]|(\\\\)*\\#\{)+", Token::NAME_DECORATOR).unwrap(), action: LexerAction::token(Token::NAME_DECORATOR) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::TEXT).unwrap(), action: LexerAction::Push("root".to_string()) },
        ]);

        ScamlLexer { inner }
    }
}

impl Lexer for ScamlLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
