// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: LdifLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated LdifLexer lexer.
pub struct LdifLexer {
    inner: RegexLexer,
}

impl LdifLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("LDIF");
        inner.aliases = vec![
            "ldif",
        ];
        inner.filenames = vec![
            "*.ldif",
        ];
        inner.mimetypes = vec![
            "text/x-ldif",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: after-control
        inner.states.insert("after-control".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r":<", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"::?", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: property
        inner.states.insert("property".to_string(), vec![
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
        ]);

        // State: url
        inner.states.insert("url".to_string(), vec![
        ]);

        // State: dn
        inner.states.insert("dn".to_string(), vec![
        ]);

        // State: dn-value
        inner.states.insert("dn-value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\+", Token::OPERATOR).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^,\+\n]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: base64-dn
        inner.states.insert("base64-dn".to_string(), vec![
        ]);

        LdifLexer { inner }
    }
}

impl Lexer for LdifLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
