// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: BefungeLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated BefungeLexer lexer.
pub struct BefungeLexer {
    inner: RegexLexer,
}

impl BefungeLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Befunge");
        inner.aliases = vec![
            "befunge",
        ];
        inner.filenames = vec![
            "*.befunge",
        ];
        inner.mimetypes = vec![
            "application/x-befunge",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9a-f]", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[+*/%!`-]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[<>^v?\[\]rxjk]", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[:\\$.,n]", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[|_mw]", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[{}]", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r##"".*?""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\'.", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[#;]", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[pg&~=@iotsy]", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[()A-Z]", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });

        inner.states.insert("root".to_string(), root_rules);

        BefungeLexer { inner }
    }
}

impl Lexer for BefungeLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
