// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: VerifpalLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated VerifpalLexer lexer.
pub struct VerifpalLexer {
    inner: RegexLexer,
}

impl VerifpalLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Verifpal");
        inner.aliases = vec![
            "verifpal",
        ];
        inner.filenames = vec![
            "*.vp",
        ];
        inner.mimetypes = vec![
            "text/x-verifpal",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"//.*$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"((?:generate|leak)s)\b", Token::NAME_BUILTIN).unwrap(), action: LexerAction::Push("shared".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(p(?:hase|recondition))\b", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[\[\(\)\]\?:=→^,]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"->", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(password)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(A(?:EAD_(?:(?:DE|EN)C)|SSERT)|BLIND|CONCAT|DEC|ENC|H(?:ASH|KDF)|MAC|P(?:KE_(?:(?:DE|EN)C)|W_HASH)|RINGSIGN(?:(?:VERIF)?)|S(?:HAMIR_(?:JOIN|SPLIT)|IGN(?:(?:VERIF)?)|PLIT)|UNBLIND|nil|[G_])\b", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\w+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) });

        inner.states.insert("root".to_string(), root_rules);

        // State: shared
        inner.states.insert("shared".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[\^\[\],]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\w+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        // State: queries
        inner.states.insert("queries".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        VerifpalLexer { inner }
    }
}

impl Lexer for VerifpalLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
