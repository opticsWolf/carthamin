// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: HexdumpLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated HexdumpLexer lexer.
pub struct HexdumpLexer {
    inner: RegexLexer,
}

impl HexdumpLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Hexdump");
        inner.aliases = vec![
            "hexdump",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9A-Ha-h]{2}", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^\*", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });

        inner.states.insert("root".to_string(), root_rules);

        // State: offset
        inner.states.insert("offset".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"^[0-9A-Ha-h]+", Token::NAME_LABEL).unwrap(), action: LexerAction::token(Token::NAME_LABEL) },
        ]);

        // State: offset-mode
        inner.states.insert("offset-mode".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[0-9A-Ha-h]+", Token::NAME_LABEL).unwrap(), action: LexerAction::token(Token::NAME_LABEL) },
            LexerRule { pattern: TokenPattern::new(r":", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: piped-strings
        inner.states.insert("piped-strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[0-9A-Ha-h]{2}", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"^\*", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: bracket-strings
        inner.states.insert("bracket-strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[0-9A-Ha-h]{2}", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"^\*", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: nonpiped-strings
        inner.states.insert("nonpiped-strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[0-9A-Ha-h]{2}", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"^\*", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        HexdumpLexer { inner }
    }
}

impl Lexer for HexdumpLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
