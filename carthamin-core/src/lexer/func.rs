// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: FuncLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated FuncLexer lexer.
pub struct FuncLexer {
    inner: RegexLexer,
}

impl FuncLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("FunC");
        inner.aliases = vec![
            "func",
            "fc",
        ];
        inner.filenames = vec![
            "*.fc",
            "*.func",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[.;(),\[\]~{}]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });

        inner.states.insert("root".to_string(), root_rules);

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\s)(!=|%=|\&=|\*=|\+=|\-=|/(?:[%=])|<(?:<=|=>|[<=])|==|>(?:>=|[=>])|\^(?:%=|/=|>>(?:(?:=)?)|[%/=])|\|=|\~(?:/=|>>(?:(?:=)?)|[%/])|[%&*+\-/:<=>?\^|~])(?=\s)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\b(asm|do|else(?:(?:if(?:(?:not)?))?)|forall|i(?:f(?:(?:not)?)|mpure|nline(?:(?:_ref)?))|method_id|re(?:peat|turn)|until|while)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b((?:fals|tru)e)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
        ]);

        // State: directives
        inner.states.insert("directives".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#include|#pragma", Token::KEYWORD).unwrap(), action: LexerAction::Push("directive".to_string()) },
        ]);

        // State: directive
        inner.states.insert("directive".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"version|not-version", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"(>=|<=|=|>|<|\^)?([0-9]+)(.[0-9]+)?(.[0-9]+)?", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r";", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: strings
        inner.states.insert("strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"\"([^\n\"]+)\"[Hhcusa]?"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: numeric
        inner.states.insert("numeric".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(-?(?!_)([\d_]+|0x[\d_a-fA-F]+)|0b[1_0]+)(?<!_)(?=[\s\)\],;])", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
        ]);

        // State: comments
        inner.states.insert("comments".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{-", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("comment".to_string()) },
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^-}{]+", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"\{-", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"-\}", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[-}{]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: storage
        inner.states.insert("storage".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(_|builder|c(?:ell|ont)|int|slice|tuple|var)(?=[\s\(\),\[\]])", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
            LexerRule { pattern: TokenPattern::new(r"\b(const|global)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
        ]);

        // State: variables
        inner.states.insert("variables".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"(?!")(`([^`]+)`|((?=_)_|(?=\{)\{|(?=\})\}|(?![_`{}]))([^;,\[\]\(\)\s~.]+))"#, Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        // State: functions
        inner.states.insert("functions".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"(?!")(`([^`]+)`|((?=_)_|(?=\{)\{|(?=\})\}|(?![_`{}]))([^;,\[\]\(\)\s~.]+))(?=[\(])"#, Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
        ]);

        FuncLexer { inner }
    }
}

impl Lexer for FuncLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
