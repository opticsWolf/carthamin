// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: Dasm16Lexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated Dasm16Lexer lexer.
pub struct Dasm16Lexer {
    inner: RegexLexer,
}

impl Dasm16Lexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("DASM16");
        inner.aliases = vec![
            "dasm16",
        ];
        inner.filenames = vec![
            "*.dasm16",
            "*.dasm",
        ];
        inner.mimetypes = vec![
            "text/x-dasm16",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r":(?:[a-zA-Z$_][a-zA-Z0-9_$@.]*|\.[a-zA-Z0-9_$@.]+)", Token::NAME_LABEL).unwrap(), action: LexerAction::token(Token::NAME_LABEL) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?:[a-zA-Z$_][a-zA-Z0-9_$@.]*|\.[a-zA-Z0-9_$@.]+):", Token::NAME_LABEL).unwrap(), action: LexerAction::token(Token::NAME_LABEL) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(SET|ADD|SUB|MUL|MLI|DIV|DVI|MOD|MDI|AND|BOR|XOR|SHR|ASR|SHL|IFB|IFC|IFE|IFN|IFG|IFA|IFL|IFU|ADX|SBX|STI|STD|JSR|INT|IAG|IAS|RFI|IAQ|HWN|HWQ|HWI)", Token::NAME_FUNCTION).unwrap(), action: LexerAction::Push("instruction-args".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\.(?:[a-zA-Z$_][a-zA-Z0-9_$@.]*|\.[a-zA-Z0-9_$@.]+)", Token::NAME_FUNCTION).unwrap(), action: LexerAction::Push("data-args".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: numeric
        inner.states.insert("numeric".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"0b[01_]+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
            LexerRule { pattern: TokenPattern::new(r"[+-]?(?:0[xX][a-zA-Z0-9]+|\d+)", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
            LexerRule { pattern: TokenPattern::new(r"'\\?[a-zA-Z0-9_$@.]'", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: arg
        inner.states.insert("arg".to_string(), vec![
        ]);

        // State: deref
        inner.states.insert("deref".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\+", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\]", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: instruction-line
        inner.states.insert("instruction-line".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";.*?$", Token::COMMENT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: instruction-args
        inner.states.insert("instruction-args".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\[", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("deref".to_string()) },
        ]);

        // State: data-args
        inner.states.insert("data-args".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r##""(\\"|[^"])*""##, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";.*?\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
        ]);

        Dasm16Lexer { inner }
    }
}

impl Lexer for Dasm16Lexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
