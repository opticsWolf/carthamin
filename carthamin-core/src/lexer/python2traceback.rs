// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: Python2TracebackLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated Python2TracebackLexer lexer.
pub struct Python2TracebackLexer {
    inner: RegexLexer,
}

impl Python2TracebackLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Python 2.x Traceback");
        inner.aliases = vec![
            "py2tb",
        ];
        inner.filenames = vec![
            "*.py2tb",
        ];
        inner.mimetypes = vec![
            "text/x-python2-traceback",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"^(?=  File "[^"]+", line \d+)"#, Token::GENERIC_TRACEBACK).unwrap(), action: LexerAction::Push("intb".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^.*\n", Token::OTHER).unwrap(), action: LexerAction::token(Token::OTHER) });

        inner.states.insert("root".to_string(), root_rules);

        // State: intb
        inner.states.insert("intb".to_string(), vec![
        ]);

        // State: marker
        inner.states.insert("marker".to_string(), vec![
        ]);

        Python2TracebackLexer { inner }
    }
}

impl Lexer for Python2TracebackLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
