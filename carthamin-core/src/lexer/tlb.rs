// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: TlbLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated TlbLexer lexer.
pub struct TlbLexer {
    inner: RegexLexer,
}

impl TlbLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Tl-b");
        inner.aliases = vec![
            "tlb",
        ];
        inner.filenames = vec![
            "*.tlb",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9]+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(!=|(?:[<=>])=|[*+\-.<=>?\^~])", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(\#(?:<=|[#<]))", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#[0-9a-f]*_?", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\$[01]*_?", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[a-zA-Z_][0-9a-zA-Z_]*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[;():\[\]{}]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });

        inner.states.insert("root".to_string(), root_rules);

        // State: comments
        inner.states.insert("comments".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"/\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("comment".to_string()) },
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^/*]+", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"/\*", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\*/", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[*/]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        TlbLexer { inner }
    }
}

impl Lexer for TlbLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
