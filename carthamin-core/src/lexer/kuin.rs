// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: KuinLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated KuinLexer lexer.
pub struct KuinLexer {
    inner: RegexLexer,
}

impl KuinLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Kuin");
        inner.aliases = vec![
            "kuin",
        ];
        inner.filenames = vec![
            "*.kn",
        ];

        // State: statement
        inner.states.insert("statement".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(do)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("do".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"(\+?[ \t]*\bvar)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("var".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\b(const)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("const".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\b(ret)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("ret".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\b(throw)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("throw".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\b(alias)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("alias".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\b(assert)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("assert".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\|", Token::TEXT).unwrap(), action: LexerAction::Push("continued_line".to_string()) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("multiline_comment".to_string()) },
        ]);

        // State: multiline_comment
        inner.states.insert("multiline_comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("multiline_comment".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"(?:\s*;.*|[^{}\n]+)", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"\}", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: func_
        inner.states.insert("func_".to_string(), vec![
        ]);

        // State: func
        inner.states.insert("func".to_string(), vec![
        ]);

        // State: class_
        inner.states.insert("class_".to_string(), vec![
        ]);

        // State: class
        inner.states.insert("class".to_string(), vec![
        ]);

        // State: enum_
        inner.states.insert("enum_".to_string(), vec![
        ]);

        // State: enum
        inner.states.insert("enum".to_string(), vec![
        ]);

        // State: block_
        inner.states.insert("block_".to_string(), vec![
        ]);

        // State: block
        inner.states.insert("block".to_string(), vec![
        ]);

        // State: ifdef_
        inner.states.insert("ifdef_".to_string(), vec![
        ]);

        // State: ifdef
        inner.states.insert("ifdef".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(dbg|rls)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::Push("ifdef_sp".to_string()) },
        ]);

        // State: ifdef_sp
        inner.states.insert("ifdef_sp".to_string(), vec![
        ]);

        // State: if_
        inner.states.insert("if_".to_string(), vec![
        ]);

        // State: if
        inner.states.insert("if".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(el(?:if|se))\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("if_sp".to_string()) },
        ]);

        // State: if_sp
        inner.states.insert("if_sp".to_string(), vec![
        ]);

        // State: switch_
        inner.states.insert("switch_".to_string(), vec![
        ]);

        // State: switch
        inner.states.insert("switch".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(case|default|to)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("switch_sp".to_string()) },
        ]);

        // State: switch_sp
        inner.states.insert("switch_sp".to_string(), vec![
        ]);

        // State: while_
        inner.states.insert("while_".to_string(), vec![
        ]);

        // State: while
        inner.states.insert("while".to_string(), vec![
        ]);

        // State: for_
        inner.states.insert("for_".to_string(), vec![
        ]);

        // State: for
        inner.states.insert("for".to_string(), vec![
        ]);

        // State: foreach_
        inner.states.insert("foreach_".to_string(), vec![
        ]);

        // State: foreach
        inner.states.insert("foreach".to_string(), vec![
        ]);

        // State: try_
        inner.states.insert("try_".to_string(), vec![
        ]);

        // State: try
        inner.states.insert("try".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(catch|finally|to)\b", Token::KEYWORD).unwrap(), action: LexerAction::Push("try_sp".to_string()) },
        ]);

        // State: try_sp
        inner.states.insert("try_sp".to_string(), vec![
        ]);

        // State: break
        inner.states.insert("break".to_string(), vec![
        ]);

        // State: skip
        inner.states.insert("skip".to_string(), vec![
        ]);

        // State: alias
        inner.states.insert("alias".to_string(), vec![
        ]);

        // State: assert
        inner.states.insert("assert".to_string(), vec![
        ]);

        // State: const
        inner.states.insert("const".to_string(), vec![
        ]);

        // State: do
        inner.states.insert("do".to_string(), vec![
        ]);

        // State: ret
        inner.states.insert("ret".to_string(), vec![
        ]);

        // State: throw
        inner.states.insert("throw".to_string(), vec![
        ]);

        // State: var
        inner.states.insert("var".to_string(), vec![
        ]);

        // State: continued_line
        inner.states.insert("continued_line".to_string(), vec![
        ]);

        // State: expr
        inner.states.insert("expr".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\(", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\)", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\[", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\b(false|inf|null|true)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"\b([em])\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r"\b(b(?:it(?:16|32|64|8)|ool)|c(?:har|lass)|dict|enum|f(?:loat|unc)|int|list|queue|stack)\b", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
            LexerRule { pattern: TokenPattern::new(r"\b[0-9]\.[0-9]+(?!\.)(:?e[\+-][0-9]+)?\b", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) },
            LexerRule { pattern: TokenPattern::new(r"\b2#[01]+(?:b(?:8|16|32|64))?\b", Token::NUMBER_BIN).unwrap(), action: LexerAction::token(Token::NUMBER_BIN) },
            LexerRule { pattern: TokenPattern::new(r"\b8#[0-7]+(?:b(?:8|16|32|64))?\b", Token::NUMBER_OCT).unwrap(), action: LexerAction::token(Token::NUMBER_OCT) },
            LexerRule { pattern: TokenPattern::new(r"\b16#[0-9A-F]+(?:b(?:8|16|32|64))?\b", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::Push("string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"'(?:\\.|.)+?'", Token::STRING_CHAR).unwrap(), action: LexerAction::token(Token::STRING_CHAR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\.|\$(?:>|<)?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\^)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\+|-|!|##?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\*|/|%)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:~)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:(?:=|<>)(?:&|\$)?|<=?|>=?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:&)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\|)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?:\?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?::(?::|\+|-|\*|/|%|\^|~)?)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\b([a-zA-Z_][0-9a-zA-Z_]*)(?=@)\b", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
        ]);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"(?:\\[^{\n]|[^"\\])+"#, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
            LexerRule { pattern: TokenPattern::new(r"\\\{", Token::STRING_DOUBLE).unwrap(), action: LexerAction::Push("toStrInString".to_string()) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: toStrInString
        inner.states.insert("toStrInString".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::STRING_DOUBLE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        KuinLexer { inner }
    }
}

impl Lexer for KuinLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
