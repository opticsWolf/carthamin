// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: VueLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated VueLexer lexer.
pub struct VueLexer {
    inner: RegexLexer,
}

impl VueLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Vue");
        inner.aliases = vec![
            "vue",
        ];
        inner.filenames = vec![
            "*.vue",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^<&{]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^<&]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"&\S*?;", Token::NAME_ENTITY).unwrap(), action: LexerAction::token(Token::NAME_ENTITY) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\<\!\[CDATA\[.*?\]\]\>", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<!--.*?-->", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\?.*?\?>", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<![^>]*>", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });

        inner.states.insert("root".to_string(), root_rules);

        // State: tag
        inner.states.insert("tag".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"[\w:-]+", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
        ]);

        // State: name
        inner.states.insert("name".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[\w-]+", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
            LexerRule { pattern: TokenPattern::new(r"[:@.]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: attr-directive
        inner.states.insert("attr-directive".to_string(), vec![
        ]);

        // State: script-content
        inner.states.insert("script-content".to_string(), vec![
        ]);

        // State: style-content
        inner.states.insert("style-content".to_string(), vec![
        ]);

        // State: attr
        inner.states.insert("attr".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"".*?""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"'.*?'", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^\s>]+", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        VueLexer { inner }
    }
}

impl Lexer for VueLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
