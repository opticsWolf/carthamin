// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: IrcLogsLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated IrcLogsLexer lexer.
pub struct IrcLogsLexer {
    inner: RegexLexer,
}

impl IrcLogsLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("IRC logs");
        inner.aliases = vec![
            "irc",
        ];
        inner.filenames = vec![
            "*.weechatlog",
        ];
        inner.mimetypes = vec![
            "text/x-irclog",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^\*\*\*\*(.*)\*\*\*\*$", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^.*?\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: msg
        inner.states.insert("msg".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\S+:(?!//)", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
            LexerRule { pattern: TokenPattern::new(r".*\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        IrcLogsLexer { inner }
    }
}

impl Lexer for IrcLogsLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
