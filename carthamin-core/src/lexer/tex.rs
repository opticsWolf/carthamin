// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: TexLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated TexLexer lexer.
pub struct TexLexer {
    inner: RegexLexer,
}

impl TexLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("TeX");
        inner.aliases = vec![
            "tex",
            "latex",
        ];
        inner.filenames = vec![
            "*.tex",
            "*.aux",
            "*.toc",
        ];
        inner.mimetypes = vec![
            "text/x-tex",
            "text/x-latex",
        ];

        // State: general
        inner.states.insert("general".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%.*?\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"[{}]", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
            LexerRule { pattern: TokenPattern::new(r"[&_^]", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\\\[", Token::STRING_BACKTICK).unwrap(), action: LexerAction::Push("displaymath".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\\\(", Token::STRING).unwrap(), action: LexerAction::Push("inlinemath".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\$\$", Token::STRING_BACKTICK).unwrap(), action: LexerAction::Push("displaymath".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\$", Token::STRING).unwrap(), action: LexerAction::Push("inlinemath".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\\([a-zA-Z@_:]+|\S?)", Token::KEYWORD).unwrap(), action: LexerAction::Push("command".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\\$", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^\\$%&_^{}]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: math
        inner.states.insert("math".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\([a-zA-Z]+|\S?)", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r"[0-9]+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"[-=!+*/()\[\]]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"[^=!+*/()\[\]\\$%&_^{}0-9-]+", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: inlinemath
        inner.states.insert("inlinemath".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\)", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\$", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: displaymath
        inner.states.insert("displaymath".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\]", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\$\$", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\$", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: command
        inner.states.insert("command".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\[.*?\]", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
            LexerRule { pattern: TokenPattern::new(r"\*", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        TexLexer { inner }
    }
}

impl Lexer for TexLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
