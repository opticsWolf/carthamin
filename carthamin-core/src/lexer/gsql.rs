// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: GSQLLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated GSQLLexer lexer.
pub struct GSQLLexer {
    inner: RegexLexer,
}

impl GSQLLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("GSQL");
        inner.aliases = vec![
            "gsql",
        ];
        inner.filenames = vec![
            "*.gsql",
        ];

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\#.*", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"/\*(.|\n)*?\*/", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<!\.)(A(?:CCUM|N(?:[DY])|PI|S(?:(?:C)?)|VG)|B(?:A(?:G|TCH)|ETWEEN|O(?:OL|TH)|REAK|Y)|C(?:A(?:SE|TCH)|O(?:ALESCE|MPRESS|NTINUE|UNT)|REATE)|D(?:ATETIME(?:(?:_(?:ADD|SUB))?)|E(?:LETE|SC)|ISTRIBUTED|O(?:(?:UBLE)?))|E(?:DGE|LSE|ND|SCAPE|XCEPTION)|F(?:ALSE|IL(?:E|TER)|LOAT|OR(?:(?:EACH)?)|ROM)|G(?:R(?:APH|OUP)|SQL_(?:INT_M(?:AX|IN)|UINT_MAX))|HAVING|I(?:N(?:SERT|T(?:(?:ER(?:PRET|SECT|VAL)|O)?))|SEMPTY|[FNS])|JSON(?:ARRAY|OBJECT)|L(?:ASTHOP|EADING|I(?:KE|(?:MI|S)T)|O(?:AD_ACCUM|G))|M(?:A(?:TCH|[PX])|IN(?:(?:US)?))|N(?:O(?:[TW])|ULL)|O(?:FFSET|R(?:(?:DER)?))|P(?:ATH|ER|INNED|OST(?:(?:[\-_])ACCUM)|RI(?:MARY_ID|NT))|QUERY|R(?:A(?:(?:IS|NG)E)|E(?:PLACE|SET_COLLECTION_ACCUM|TURN(?:(?:S)?))|UN)|S(?:AMPLE|E(?:LECT(?:(?:_VERTEX)?)|T)|RC|T(?:ATIC|RING)|UM|YNTAX)|T(?:A(?:(?:GSTG|RGE)T)|HEN|O(?:(?:_(?:CSV|DATETIME))?)|R(?:AILING|IM|UE|Y)|UPLE|YPEDEF)|U(?:INT|NION|PDATE)|V(?:ALUES|ERTEX)|W(?:H(?:E(?:N|RE)|ILE)|ITH))\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: clauses
        inner.states.insert("clauses".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(accum|having|limit|order|postAccum|(?:sampl|wher)e)", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: accums
        inner.states.insert("accums".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"((?:MapA|(?:a(?:nd|rray|vg)|b(?:ag|itwise(?:and|or))|groupby|heap|list|m(?:ax|in)|or|s(?:et|um))a)ccum)", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: relations
        inner.states.insert("relations".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"->|<-", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"[.*{}\[\]\<\>\_]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: strings
        inner.states.insert("strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##""([^"\\]|\\.)*""##, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"@{1,2}\w+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        // State: barewords
        inner.states.insert("barewords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[a-z]\w*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
            LexerRule { pattern: TokenPattern::new(r"(\d+\.\d+|\d+)", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
        ]);

        // State: operators
        inner.states.insert("operators".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\$|[^0-9|\/|\-](\-\=|\+\=|\*\=|\\\=|\=|\=\=|\=\=\=|\+|\-|\*|\\|\+\=|\>|\<)[^\>|\/]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(\||\(|\)|\,|\;|\=|\-|\+|\*|\/|\>|\<|\:)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
        ]);

        GSQLLexer { inner }
    }
}

impl Lexer for GSQLLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
