// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: CsoundDocumentLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated CsoundDocumentLexer lexer.
pub struct CsoundDocumentLexer {
    inner: RegexLexer,
}

impl CsoundDocumentLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Csound Document");
        inner.aliases = vec![
            "csound-document",
            "csound-csd",
        ];
        inner.filenames = vec![
            "*.csd",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"/[*](.|\n)*?[*]/", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?:;|//).*$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^/;<]+|/(?!/)", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\s*CsInstruments", Token::NAME_TAG).unwrap(), action: LexerAction::Push("orchestra".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\s*CsScore", Token::NAME_TAG).unwrap(), action: LexerAction::Push("score".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\s*[Hh][Tt][Mm][Ll]", Token::NAME_TAG).unwrap(), action: LexerAction::Push("HTML".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\s*[\w:.-]+", Token::NAME_TAG).unwrap(), action: LexerAction::Push("tag".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<\s*/\s*[\w:.-]+\s*>", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) });

        inner.states.insert("root".to_string(), root_rules);

        // State: orchestra
        inner.states.insert("orchestra".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<\s*/\s*CsInstruments\s*>", Token::NAME_TAG).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: score
        inner.states.insert("score".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<\s*/\s*CsScore\s*>", Token::NAME_TAG).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: HTML
        inner.states.insert("HTML".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<\s*/\s*[Hh][Tt][Mm][Ll]\s*>", Token::NAME_TAG).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: tag
        inner.states.insert("tag".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[\w.:-]+\s*=", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::Push("attr".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"/?\s*>", Token::NAME_TAG).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: attr
        inner.states.insert("attr".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"".*?""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"'.*?'", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^\s>]+", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        CsoundDocumentLexer { inner }
    }
}

impl Lexer for CsoundDocumentLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
