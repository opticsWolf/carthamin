// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: FortranFixedLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated FortranFixedLexer lexer.
pub struct FortranFixedLexer {
    inner: RegexLexer,
}

impl FortranFixedLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("FortranFixed");
        inner.aliases = vec![
            "fortranfixed",
        ];
        inner.filenames = vec![
            "*.f",
            "*.F",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[C*].*\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#.*\n", Token::COMMENT_PREPROC).unwrap(), action: LexerAction::token(Token::COMMENT_PREPROC) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r" {0,4}!.*\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(.{5})", Token::NAME_LABEL).unwrap(), action: LexerAction::Push("cont-char".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: cont-char
        inner.states.insert("cont-char".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r" ", Token::TEXT).unwrap(), action: LexerAction::Push("code".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"0", Token::COMMENT).unwrap(), action: LexerAction::Push("code".to_string()) },
            LexerRule { pattern: TokenPattern::new(r".", Token::GENERIC_STRONG).unwrap(), action: LexerAction::Push("code".to_string()) },
        ]);

        // State: code
        inner.states.insert("code".to_string(), vec![
        ]);

        FortranFixedLexer { inner }
    }
}

impl Lexer for FortranFixedLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
