// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: ApacheConfLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated ApacheConfLexer lexer.
pub struct ApacheConfLexer {
    inner: RegexLexer,
}

impl ApacheConfLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("ApacheConf");
        inner.aliases = vec![
            "apacheconf",
            "aconf",
            "apache",
        ];
        inner.filenames = vec![
            ".htaccess",
            "apache.conf",
            "apache2.conf",
        ];
        inner.mimetypes = vec![
            "text/x-apacheconf",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#(.*\\\n)+.*$|(#.*?)$", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[a-z]\w*", Token::NAME_BUILTIN).unwrap(), action: LexerAction::Push("value".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\.+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\\n", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\\", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\d+\.\d+\.\d+\.\d+(?:/\d+)?", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"\d+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
            LexerRule { pattern: TokenPattern::new(r"/([*a-z0-9][*\w./-]+)", Token::STRING_OTHER).unwrap(), action: LexerAction::token(Token::STRING_OTHER) },
            LexerRule { pattern: TokenPattern::new(r"(on|off|none|any|all|double|email|dns|min|minimal|os|productonly|full|emerg|alert|crit|error|warn|notice|info|debug|registry|script|inetd|standalone|user|group)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
            LexerRule { pattern: TokenPattern::new(r##""([^"\\]*(?:\\(.|\n)[^"\\]*)*)""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
            LexerRule { pattern: TokenPattern::new(r#"[^\s"\\]+"#, Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        ApacheConfLexer { inner }
    }
}

impl Lexer for ApacheConfLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
