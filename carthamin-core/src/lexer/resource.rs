// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: ResourceLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated ResourceLexer lexer.
pub struct ResourceLexer {
    inner: RegexLexer,
}

impl ResourceLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("ResourceBundle");
        inner.aliases = vec![
            "resourcebundle",
            "resource",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"//.*?$", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::Push("string".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"-?\d+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[,{}]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(:(?:a(?:lias|rray)|bin|i(?:mport|nt(?:(?:vector)?))|string|table))", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });

        inner.states.insert("root".to_string(), root_rules);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"(\\x[0-9a-f]{2}|\\u[0-9a-f]{4}|\\U00[0-9a-f]{6}|\\[0-7]{1,3}|\\c.|\\[abtnvfre\'"?\\]|\\\{|[^"{\\])+"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\{", Token::STRING_ESCAPE).unwrap(), action: LexerAction::Push("msgname".to_string()) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: msgname
        inner.states.insert("msgname".to_string(), vec![
        ]);

        // State: message
        inner.states.insert("message".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::STRING_ESCAPE).unwrap(), action: LexerAction::Push("msgname".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\}", Token::STRING_ESCAPE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
        ]);

        // State: choice
        inner.states.insert("choice".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::STRING_ESCAPE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
        ]);

        // State: str
        inner.states.insert("str".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::STRING_ESCAPE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\{", Token::STRING_ESCAPE).unwrap(), action: LexerAction::Push("msgname".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[^{}]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        ResourceLexer { inner }
    }
}

impl Lexer for ResourceLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
