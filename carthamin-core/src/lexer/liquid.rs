// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: LiquidLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated LiquidLexer lexer.
pub struct LiquidLexer {
    inner: RegexLexer,
}

impl LiquidLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("liquid");
        inner.aliases = vec![
            "liquid",
        ];
        inner.filenames = vec![
            "*.liquid",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^{]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\{", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: tag-or-block
        inner.states.insert("tag-or-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(if|unless|elsif|case)(?=\s+)", Token::KEYWORD_RESERVED).unwrap(), action: LexerAction::Push("condition".to_string()) },
        ]);

        // State: output
        inner.states.insert("output".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\|", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("filters".to_string()) },
        ]);

        // State: filters
        inner.states.insert("filters".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: filter-markup
        inner.states.insert("filter-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\|", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: condition
        inner.states.insert("condition".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b!", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\bnot\b", Token::OPERATOR_WORD).unwrap(), action: LexerAction::token(Token::OPERATOR_WORD) },
        ]);

        // State: operator
        inner.states.insert("operator".to_string(), vec![
        ]);

        // State: end-of-tag
        inner.states.insert("end-of-tag".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: end-of-block
        inner.states.insert("end-of-block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: end-at-whitespace
        inner.states.insert("end-at-whitespace".to_string(), vec![
        ]);

        // State: param-markup
        inner.states.insert("param-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: default-param-markup
        inner.states.insert("default-param-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: variable-param-markup
        inner.states.insert("variable-param-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        // State: tag-markup
        inner.states.insert("tag-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: variable-tag-markup
        inner.states.insert("variable-tag-markup".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"%\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: keyword
        inner.states.insert("keyword".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b(false|true)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
        ]);

        // State: variable
        inner.states.insert("variable".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[a-zA-Z_]\w*", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r"(?<=\w)\.(?=\w)", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"'[^']*'", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r##""[^"]*""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::token(Token::STRING_DOUBLE) },
        ]);

        // State: number
        inner.states.insert("number".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\d+\.\d+", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) },
            LexerRule { pattern: TokenPattern::new(r"\d+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
        ]);

        // State: raw
        inner.states.insert("raw".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^{]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\{", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        LiquidLexer { inner }
    }
}

impl Lexer for LiquidLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
