// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: SrcinfoLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated SrcinfoLexer lexer.
pub struct SrcinfoLexer {
    inner: RegexLexer,
}

impl SrcinfoLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Srcinfo");
        inner.aliases = vec![
            "srcinfo",
        ];
        inner.filenames = vec![
            ".SRCINFO",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#.*", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(arch|backup|changelog|epoch|groups|install|license|noextract|options|pkg(?:base|desc|name|rel|ver)|url|validpgpkeys)", Token::KEYWORD).unwrap(), action: LexerAction::Push("assignment".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(c(?:(?:heckdepend|onflict)s)|depends|m(?:(?:akedepend|d5sum)s)|optdepends|provides|replaces|s(?:ha(?:(?:1|2(?:24|56)|384|512)sums)|ource))_\w+", Token::KEYWORD).unwrap(), action: LexerAction::Push("assignment".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\w+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::Push("assignment".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: assignment
        inner.states.insert("assignment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"=", Token::OPERATOR).unwrap(), action: LexerAction::Push("value".to_string()) },
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".*", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        SrcinfoLexer { inner }
    }
}

impl Lexer for SrcinfoLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
