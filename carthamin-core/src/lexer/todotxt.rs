// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: TodotxtLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated TodotxtLexer lexer.
pub struct TodotxtLexer {
    inner: RegexLexer,
}

impl TodotxtLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Todotxt");
        inner.aliases = vec![
            "todotxt",
        ];
        inner.filenames = vec![
            "todo.txt",
            "*.todotxt",
        ];
        inner.mimetypes = vec![
            "text/x-todo",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\([A-Z]\)", Token::GENERIC_HEADING).unwrap(), action: LexerAction::Push("incomplete".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\d{4,}-\d{2}-\d{2}", Token::GENERIC_SUBHEADING).unwrap(), action: LexerAction::Push("incomplete".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"@\S+", Token::STRING).unwrap(), action: LexerAction::Push("incomplete".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\+\S+", Token::GENERIC_ERROR).unwrap(), action: LexerAction::Push("incomplete".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\S+", Token::TEXT).unwrap(), action: LexerAction::Push("incomplete".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: complete
        inner.states.insert("complete".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s*\n", Token::OPERATOR).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"@\S+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\+\S+", Token::GENERIC_ERROR).unwrap(), action: LexerAction::token(Token::GENERIC_ERROR) },
            LexerRule { pattern: TokenPattern::new(r"\S+", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
        ]);

        // State: incomplete
        inner.states.insert("incomplete".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\s*\n", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"@\S+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\+\S+", Token::GENERIC_ERROR).unwrap(), action: LexerAction::token(Token::GENERIC_ERROR) },
            LexerRule { pattern: TokenPattern::new(r"\S+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        TodotxtLexer { inner }
    }
}

impl Lexer for TodotxtLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
