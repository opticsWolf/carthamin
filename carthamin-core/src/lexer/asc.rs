// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: AscLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated AscLexer lexer.
pub struct AscLexer {
    inner: RegexLexer,
}

impl AscLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("ASCII armored");
        inner.aliases = vec![
            "asc",
            "pem",
        ];
        inner.filenames = vec![
            "*.asc",
            "*.pem",
            "id_dsa",
            "id_ecdsa",
            "id_ecdsa_sk",
            "id_ed25519",
            "id_ed25519_sk",
            "id_rsa",
        ];
        inner.mimetypes = vec![
            "application/pgp-keys",
            "application/pgp-encrypted",
            "application/pgp-signature",
            "application/pem-certificate-chain",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"^-----BEGIN [^\n]+-----$", Token::GENERIC_HEADING).unwrap(), action: LexerAction::Push("data".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\S+", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: data
        inner.states.insert("data".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"^-----END [^\n]+-----$", Token::GENERIC_HEADING).unwrap(), action: LexerAction::Push("root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\S+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        AscLexer { inner }
    }
}

impl Lexer for AscLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
