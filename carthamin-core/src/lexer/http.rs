// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: HttpLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated HttpLexer lexer.
pub struct HttpLexer {
    inner: RegexLexer,
}

impl HttpLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("HTTP");
        inner.aliases = vec![
            "http",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: headers
        inner.states.insert("headers".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\r?\n", Token::TEXT).unwrap(), action: LexerAction::Push("content".to_string()) },
        ]);

        // State: content
        inner.states.insert("content".to_string(), vec![
        ]);

        HttpLexer { inner }
    }
}

impl Lexer for HttpLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
