// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: RegeditLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated RegeditLexer lexer.
pub struct RegeditLexer {
    inner: RegexLexer,
}

impl RegeditLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("reg");
        inner.aliases = vec![
            "registry",
        ];
        inner.filenames = vec![
            "*.reg",
        ];
        inner.mimetypes = vec![
            "text/x-windows-registry",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"Windows Registry Editor.*", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[;#].*", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });

        inner.states.insert("root".to_string(), root_rules);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"-", Token::OPERATOR).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r".+", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        RegeditLexer { inner }
    }
}

impl Lexer for RegeditLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
