// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: MIMELexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated MIMELexer lexer.
pub struct MIMELexer {
    inner: RegexLexer,
}

impl MIMELexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("MIME");
        inner.aliases = vec![
            "mime",
        ];
        inner.mimetypes = vec![
            "multipart/mixed",
            "multipart/related",
            "multipart/alternative",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: header
        inner.states.insert("header".to_string(), vec![
        ]);

        // State: content-type
        inner.states.insert("content-type".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r";[ \t]*\n(?![ \t])", Token::TEXT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: content-transfer-encoding
        inner.states.insert("content-transfer-encoding".to_string(), vec![
        ]);

        MIMELexer { inner }
    }
}

impl Lexer for MIMELexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
