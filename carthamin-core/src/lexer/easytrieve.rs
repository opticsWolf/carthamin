// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: EasytrieveLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated EasytrieveLexer lexer.
pub struct EasytrieveLexer {
    inner: RegexLexer,
}

impl EasytrieveLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Easytrieve");
        inner.aliases = vec![
            "easytrieve",
        ];
        inner.filenames = vec![
            "*.ezt",
            "*.mac",
        ];
        inner.mimetypes = vec![
            "text/x-easytrieve",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\*.*\n", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"&[^ \'.,():\n*]+\.", Token::NAME_VARIABLE).unwrap(), action: LexerAction::Push("after_macro_argument".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"%[^ \'.,():\n*]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[.+\-/=\[\](){}<>;,&%¬]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9]+\.[0-9]*", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9]+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"'(''|[^'])*'", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^ \'.,():\n*]+", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) });

        inner.states.insert("root".to_string(), root_rules);

        // State: after_declaration
        inner.states.insert("after_declaration".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^ \'.,():\n*]+", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
        ]);

        // State: after_macro_argument
        inner.states.insert("after_macro_argument".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\*.*\n", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[.+\-/=\[\](){}<>;,&%¬]", Token::OPERATOR).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"'(''|[^'])*'", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^ \'.,():\n*]+", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
        ]);

        EasytrieveLexer { inner }
    }
}

impl Lexer for EasytrieveLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
