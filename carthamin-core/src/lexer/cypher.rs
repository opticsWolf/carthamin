// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: CypherLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated CypherLexer lexer.
pub struct CypherLexer {
    inner: RegexLexer,
}

impl CypherLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Cypher");
        inner.aliases = vec![
            "cypher",
        ];
        inner.filenames = vec![
            "*.cyp",
            "*.cypher",
        ];

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(create|order|match|limit|set|skip|start|return|with|where|delete|foreach|not|by|true|false)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: clauses
        inner.states.insert("clauses".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(a(?:ll|ny|s(?:(?:c(?:(?:ending)?)|sert)?))|c(?:a(?:ll|se)|ollect|reate)|d(?:e(?:lete|sc(?:(?:ending)?))|istinct)|end|f(?:ieldterminator|oreach)|in|limit|m(?:atch|erge)|n(?:o(?:ne|t)|ull)|re(?:move|turn)|s(?:et|ingle|kip|tart)|then|un(?:ion|wind)|w(?:he(?:n|re)|ith)|yield)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: relations
        inner.states.insert("relations".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"-->|<--|\[|\]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"<|>|<>|=|<=|=>|\(|\)|\||:|,|;", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"[.*{}]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: strings
        inner.states.insert("strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r#"([\'"])(?:\\[tbnrf\'"\\]|[^\\])*?\1"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"`(?:``|[^`])+`", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        // State: barewords
        inner.states.insert("barewords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[a-z]\w*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
            LexerRule { pattern: TokenPattern::new(r"\d+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) },
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"//.*$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
        ]);

        CypherLexer { inner }
    }
}

impl Lexer for CypherLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
