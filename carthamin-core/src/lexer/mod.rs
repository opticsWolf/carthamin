pub mod regex_lexer;
pub mod extended;
pub mod python;
pub mod javascript;
pub mod htmlxml;
pub mod css;
pub mod rust;
pub mod cpp;
pub mod go;
pub mod java;
pub mod sql;
pub mod bash;
pub mod csharp;
pub mod swift;
pub mod kotlin;
pub mod php;
pub mod ruby;
pub mod lua;
pub mod r;
pub mod json;
pub mod yaml;
pub mod markdown;
pub mod protobuf;
pub mod powershell;
pub mod postgres;
pub mod docker;
pub mod terraform;
pub mod makefile;
pub mod scala;
pub mod julia;
pub mod django;












pub mod abap;
pub mod abnf;
pub mod actionscript;
pub mod actionscript3;
pub mod ada;
pub mod adl;
pub mod agda;
pub mod aheui;
pub mod alloy;
pub mod ambienttalk;
pub mod amdgpu;
pub mod ampl;
pub mod angular2;
pub mod antlr;
pub mod antlrpython;
pub mod antlrruby;
pub mod apacheconf;
pub mod apdlexer;
pub mod apl;
pub mod applescript;
pub mod arduino;
pub mod arrow;
pub mod arturo;
pub mod asc;
pub mod asn1;
pub mod aspectj;
pub mod asymptote;
pub mod augeas;
pub mod autohotkey;
pub mod autoit;
pub mod awk;
pub mod bare;
pub mod basemakefile;
pub mod batch;
pub mod bbcbasic;
pub mod bbcode;
pub mod bc;
pub mod bdd;
pub mod befunge;
pub mod berry;
pub mod bibtex;
pub mod blitzbasic;
pub mod blitzmax;
pub mod blueprint;
pub mod bnf;
pub mod boa;
pub mod boo;
pub mod boogie;
pub mod bqn;
pub mod brainfuck;
pub mod bst;
pub mod bugs;
pub mod ca65;
pub mod cadl;
pub mod camkes;
pub mod capdl;
pub mod capnproto;
pub mod carbon;
pub mod cbmbasicv2;
pub mod cddl;
pub mod ceylon;
pub mod cfengine3;
pub mod chaiscript;
pub mod chapel;
pub mod charmci;
pub mod cheetah;
pub mod cheetahxml;
pub mod cirru;
pub mod clay;
pub mod clean;
pub mod clojure;
pub mod clojurescript;
pub mod cmake;
pub mod cobol;
pub mod cobolfreeformat;
pub mod codeql;
pub mod coffeescript;
pub mod coldfusion;
pub mod comal80;
pub mod commonlisp;
pub mod componentpascal;
pub mod cplint;
pub mod cpsa;
pub mod crmsh;
pub mod croc;
pub mod cryptol;
pub mod crystal;
pub mod csounddocument;
pub mod csoundorchestra;
pub mod csoundscore;
pub mod cssdjango;
pub mod cssphp;
pub mod cuda;
pub mod cypher;
pub mod cython;
pub mod darcspatch;
pub mod dart;
pub mod dasm16;
pub mod dax;
pub mod debiancontrol;
pub mod debiansources;
pub mod desktop;
pub mod devicetree;
pub mod dg;
pub mod diff;
pub mod dnszone;
pub mod dtd;
pub mod duel;
pub mod dylan;
pub mod dylanlid;
pub mod earlgrey;
pub mod easytrieve;
pub mod ebnf;
pub mod ec;
pub mod ecl;
pub mod eiffel;
pub mod elixir;
pub mod elm;
pub mod elpi;
pub mod emacslisp;
pub mod erlang;
pub mod evoque;
pub mod evoquexml;
pub mod execline;
pub mod ezhil;
pub mod factor;
pub mod fancy;
pub mod fantom;
pub mod felix;
pub mod fennel;
pub mod fift;
pub mod fishshell;
pub mod flatline;
pub mod floscript;
pub mod forth;
pub mod fortran;
pub mod fortranfixed;
pub mod foxpro;
pub mod freefem;
pub mod fsharp;
pub mod fstar;
pub mod func;
pub mod futhark;
pub mod gap;
pub mod gas;
pub mod gcode;
pub mod gdscript;
pub mod genshitext;
pub mod gettext;
pub mod gherkin;
pub mod gleam;
pub mod glshader;
pub mod gnuplot;
pub mod golo;
pub mod gooddatacl;
pub mod googlesql;
pub mod gosu;
pub mod graphql;
pub mod graphviz;
pub mod groff;
pub mod groovy;
pub mod gsql;
pub mod haml;
pub mod handlebars;
pub mod hare;
pub mod haskell;
pub mod haxe;
pub mod hexdump;
pub mod hlslshader;
pub mod hsail;
pub mod hspec;
pub mod htmldjango;
pub mod htmlphp;
pub mod http;
pub mod hxml;
pub mod hy;
pub mod hybris;
pub mod icon;
pub mod idl;
pub mod idris;
pub mod igor;
pub mod inform6;
pub mod ini;
pub mod io;
pub mod ioke;
pub mod irclogs;
pub mod isabelle;
pub mod jags;
pub mod janet;
pub mod jasmin;
pub mod javascriptdjango;
pub mod javascriptgenshi;
pub mod javascriptphp;
pub mod jcl;
pub mod jmespath;
pub mod jsgf;
pub mod jslt;
pub mod json5;
pub mod jsonnet;
pub mod jsx;
pub mod juttle;
pub mod kal;
pub mod kconfig;
pub mod kernellog;
pub mod koka;
pub mod kuin;
pub mod kusto;
pub mod lasso;
pub mod lassoxml;
pub mod ldaprc;
pub mod ldif;
pub mod lean3;
pub mod lean4;
pub mod lesscss;
pub mod lighttpdconf;
pub mod lilypond;
pub mod limbo;
pub mod liquid;
pub mod livescript;
pub mod llvm;
pub mod llvmmir;
pub mod llvmmirbody;
pub mod logos;
pub mod logtalk;
pub mod lsl;
pub mod luau;
pub mod macaulay2;
pub mod mako;
pub mod makoxml;
pub mod maple;
pub mod maql;
pub mod mask;
pub mod mason;
pub mod mathematica;
pub mod matlab;
pub mod maxima;
pub mod mcfunction;
pub mod mcschema;
pub mod meson;
pub mod mime;
pub mod minid;
pub mod miniscript;
pub mod mips;
pub mod modelica;
pub mod modula2;
pub mod moinwiki;
pub mod mojo;
pub mod monkey;
pub mod monte;
pub mod moocode;
pub mod moonscript;
pub mod mosel;
pub mod mozpreprochash;
pub mod mozpreprocjavascript;
pub mod mozpreprocpercent;
pub mod mozpreprocxul;
pub mod mql;
pub mod mscgen;
pub mod mupad;
pub mod mxml;
pub mod myghty;
pub mod myghtyxml;
pub mod mysql;
pub mod nasm;
pub mod nasmobjdump;
pub mod ncl;
pub mod nesc;
pub mod nestedtext;
pub mod newlisp;
pub mod newspeak;
pub mod nginxconf;
pub mod nimrod;
pub mod nit;
pub mod nix;
pub mod notmuch;
pub mod nsis;
pub mod numbair;
pub mod numpy;
pub mod nusmv;
pub mod objdump;
pub mod objectivec;
pub mod objectivecpp;
pub mod objectivej;
pub mod ocaml;
pub mod octave;
pub mod odin;
pub mod omgidl;
pub mod ooc;
pub mod opa;
pub mod openedge;
pub mod openscad;
pub mod org;
pub mod pacmanconf;
pub mod pan;
pub mod parasail;
pub mod pawn;
pub mod pddl;
pub mod peg;
pub mod perl;
pub mod perl6;
pub mod phix;
pub mod pig;
pub mod pike;
pub mod pkgconfig;
pub mod plpgsql;
pub mod pointless;
pub mod pony;
pub mod postgresexplain;
pub mod postscript;
pub mod povray;
pub mod praat;
pub mod procfile;
pub mod prolog;
pub mod promela;
pub mod promql;
pub mod properties;
pub mod prql;
pub mod ptx;
pub mod pug;
pub mod puppet;
pub mod pypylog;
pub mod python2;
pub mod python2traceback;
pub mod pythontraceback;
pub mod q;
pub mod qbasic;
pub mod qlik;
pub mod qml;
pub mod qvto;
pub mod racket;
pub mod ragel;
pub mod ragelembedded;
pub mod rageljava;
pub mod ragelobjectivec;
pub mod ragelruby;
pub mod rd;
pub mod reason;
pub mod rebol;
pub mod red;
pub mod redcode;
pub mod regedit;
pub mod rego;
pub mod rell;
pub mod resource;
pub mod rexx;
pub mod ride;
pub mod rita;
pub mod rnccompact;
pub mod roboconfgraph;
pub mod roboconfinstances;
pub mod rocq;
pub mod rpmspec;
pub mod rql;
pub mod rsl;
pub mod rst;
pub mod rts;
pub mod sarl;
pub mod sas;
pub mod sass;
pub mod savi;
pub mod scaml;
pub mod scdoc;
pub mod scheme;
pub mod scilab;
pub mod scss;
pub mod sed;
pub mod shen;
pub mod shexc;
pub mod sieve;
pub mod silver;
pub mod singularity;
pub mod slim;
pub mod slurmbash;
pub mod smali;
pub mod smalltalk;
pub mod smartgameformat;
pub mod smarty;
pub mod smithy;
pub mod sml;
pub mod snbt;
pub mod snobol;
pub mod snowball;
pub mod solidity;
pub mod soong;
pub mod sophia;
pub mod sourcepawn;
pub mod sourceslist;
pub mod sparql;
pub mod spice;
pub mod sqljinja;
pub mod squidconf;
pub mod srcinfo;
pub mod stan;
pub mod stata;
pub mod supercollider;
pub mod swig;
pub mod systemd;
pub mod systemverilog;
pub mod tablegen;
pub mod tact;
pub mod tads3;
pub mod tal;
pub mod tap;
pub mod tasm;
pub mod tcl;
pub mod tcsh;
pub mod teal;
pub mod teraterm;
pub mod termcap;
pub mod terminfo;
pub mod tex;
pub mod thingsdb;
pub mod thrift;
pub mod tiddlywiki5;
pub mod tlb;
pub mod tls;
pub mod todotxt;
pub mod toml;
pub mod transactsql;
pub mod tsx;
pub mod turtle;
pub mod twig;
pub mod typescript;
pub mod typoscript;
pub mod typoscriptcssdata;
pub mod typoscripthtmldata;
pub mod typst;
pub mod ucode;
pub mod ul4;
pub mod unicon;
pub mod unixconfig;
pub mod urbiscript;
pub mod urlencoded;
pub mod usd;
pub mod vala;
pub mod vbnet;
pub mod vbscript;
pub mod vcl;
pub mod vclsnippet;
pub mod vctreestatus;
pub mod velocity;
pub mod velocityxml;
pub mod verifpal;
pub mod verilog;
pub mod vgl;
pub mod vhdl;
pub mod vim;
pub mod visualprolog;
pub mod visualprologgrammar;
pub mod vue;
pub mod vyper;
pub mod wat;
pub mod wdiff;
pub mod webidl;
pub mod wgsl;
pub mod whiley;
pub mod wikitext;
pub mod wowtoc;
pub mod wren;
pub mod x10;
pub mod xmldjango;
pub mod xmlphp;
pub mod xmlsmarty;
pub mod xmlul4;
pub mod xorg;
pub mod xpp;
pub mod xquery;
pub mod xslt;
pub mod xtend;
pub mod xtlang;
pub mod yamljinja;
pub mod yang;
pub mod yara;
pub mod zeek;
pub mod zephir;
pub mod zig;


use crate::token::Token;
use crate::lexer::python::PythonLexer;
use crate::scanner::TokenPattern;

/// A token with its position in the source.
#[derive(Debug, Clone, PartialEq)]
pub struct TokenStreamItem {
    pub index: usize,
    pub token_type: Token,
    pub text: String,
}

/// Base trait for all lexers.
pub trait Lexer {
    /// Name of the lexer.
    fn name(&self) -> &str;

    /// Aliases for this lexer.
    fn aliases(&self) -> &[&str];

    /// File name patterns this lexer matches.
    fn filenames(&self) -> &[&str];

    /// MIME types this lexer matches.
    fn mimetypes(&self) -> &[&str];

    /// Analyze text and return a confidence score (0.0 to 1.0).
    fn analyse_text(&self, _text: &str) -> f64 {
        0.0
    }

    /// Tokenize the input text, yielding (index, token_type, text) tuples.
    fn get_tokens_unprocessed(&self, _text: &str) -> Vec<TokenStreamItem> {
        Vec::new()
    }

    /// Tokenize the input text, yielding (token_type, text) tuples.
    fn get_tokens(&self, text: &str) -> Vec<(Token, String)> {
        self.get_tokens_unprocessed(text)
            .into_iter()
            .map(|item| (item.token_type, item.text))
            .collect()
    }
}

/// A rule in a RegexLexer state.
#[derive(Debug, Clone)]
pub struct LexerRule {
    pub pattern: TokenPattern,
    pub action: LexerAction,
}

impl LexerRule {
    /// Create a rule with a simple token action.
    pub fn token(pattern: &str, token: Token) -> Result<Self, regex::Error> {
        Ok(LexerRule {
            pattern: TokenPattern::new(pattern, token)?,
            action: LexerAction::Noop,
        })
    }

    /// Create a rule with a push action.
    pub fn push(pattern: &str, token: Token, state: &str) -> Result<Self, regex::Error> {
        Ok(LexerRule {
            pattern: TokenPattern::new(pattern, token)?,
            action: LexerAction::Push(state.to_string()),
        })
    }

    /// Create a rule with both token and push.
    pub fn token_and_push(pattern: &str, token: Token, state: &str) -> Result<Self, regex::Error> {
        Ok(LexerRule {
            pattern: TokenPattern::new(pattern, token)?,
            action: LexerAction::TokenAndPush(token, state.to_string()),
        })
    }
}

/// Action to take when a rule matches.
#[derive(Debug, Clone)]
pub enum LexerAction {
    /// Assign a single token type.
    Token(Token),
    /// Assign different token types to capture groups.
    ByGroups(Vec<Option<Token>>),
    /// Push a new state onto the stack.
    Push(String),
    /// Pop N states from the stack.
    PopN(usize),
    /// Push a state and assign a token.
    TokenAndPush(Token, String),
    /// No action (skip this match).
    Noop,
    /// Delegate to another lexer by name.
    Using(String),
    /// Set the default fallback state when no rules match.
    Default(String),
}

impl LexerAction {
    /// Create a Token action.
    pub fn token(t: Token) -> Self { LexerAction::Token(t) }
    /// Create a Push action.
    pub fn push(state: &str) -> Self { LexerAction::Push(state.to_string()) }
    /// Create a Pop action.
    pub fn pop(n: usize) -> Self { LexerAction::PopN(n) }
    /// Create a Default action (set fallback state).
    pub fn default(state: &str) -> Self { LexerAction::Default(state.to_string()) }
}

/// A regex-based lexer with named states and rules.
pub struct RegexLexer {
    pub name: String,
    pub aliases: Vec<&'static str>,
    pub filenames: Vec<&'static str>,
    pub mimetypes: Vec<&'static str>,
    /// Named states, each containing a list of rules.
    pub states: std::collections::HashMap<String, Vec<LexerRule>>,
    /// Default initial state.
    pub initial_state: String,
    /// Default state transitions: state_name → target_state (set by `default` directive)
    pub default_states: std::collections::HashMap<String, String>,
}

impl RegexLexer {
    pub fn new(name: &str) -> Self {
        RegexLexer {
            name: name.to_string(),
            aliases: Vec::new(),
            filenames: Vec::new(),
            mimetypes: Vec::new(),
            states: std::collections::HashMap::new(),
            initial_state: "root".to_string(),
            default_states: std::collections::HashMap::new(),
        }
    }

    pub fn add_state(&mut self, name: &str, rules: Vec<LexerRule>) {
        self.states.insert(name.to_string(), rules);
    }

    pub fn add_rule(&mut self, state: &str, rule: LexerRule) {
        self.states.entry(state.to_string()).or_default().push(rule);
    }

    /// Build a scanner from a state's rules.
    fn build_scanner(&self, state: &str) -> Option<crate::scanner::RegexScanner> {
        let rules = self.states.get(state)?;
        let mut scanner = crate::scanner::RegexScanner::new();
        for rule in rules {
            scanner.add(rule.pattern.clone());
        }
        Some(scanner)
    }

    /// Core tokenization: process text through the state machine.
    pub fn tokenize(&self, text: &str) -> Vec<TokenStreamItem> {
        let mut tokens = Vec::new();
        let mut state_stack = vec![self.initial_state.clone()];
        let mut pos = 0;
        let mut index = 0;

        while pos < text.len() {
            let current_state = state_stack.last().unwrap();
            let rules = match self.states.get(current_state) {
                Some(r) => r,
                None => break, // Unknown state
            };

            let mut matched = false;

            for rule in rules {
                if let Some(m) = rule.pattern.regex.find(&text[pos..]) {
                    // Check if this match starts at current position or is the earliest
                    if m.start() == 0 {
                        let end = pos + m.end();
                        let matched_text = &text[pos..end];

                        // Apply action
                        match &rule.action {
                            LexerAction::Token(token) => {
                                tokens.push(TokenStreamItem {
                                    index,
                                    token_type: *token,
                                    text: matched_text.to_string(),
                                });
                            }
                            LexerAction::ByGroups(groups) => {
                                let captures = rule.pattern.regex.captures(&text[pos..]).unwrap();
                                let mut any_group = false;
                                for (i, _group_token) in groups.iter().enumerate() {
                                    if let Some(Some(token)) = groups.get(i) {
                                        if let Some(c) = captures.get(i + 1) {
                                            let group_text = c.as_str();
                                            if !group_text.is_empty() {
                                                tokens.push(TokenStreamItem {
                                                    index,
                                                    token_type: *token,
                                                    text: group_text.to_string(),
                                                });
                                                any_group = true;
                                            }
                                        }
                                    }
                                }
                                // If no groups produced tokens, use the whole match
                                if !any_group {
                                    tokens.push(TokenStreamItem {
                                        index,
                                        token_type: Token::TEXT,
                                        text: matched_text.to_string(),
                                    });
                                }
                            }
                            LexerAction::Noop => {
                                // Use the pattern's default token type
                                tokens.push(TokenStreamItem {
                                    index,
                                    token_type: rule.pattern.token,
                                    text: matched_text.to_string(),
                                });
                            }
                            LexerAction::Using(_lexer_name) => {
                                // Using requires ExtendedRegexLexer with a factory;
                                // in basic RegexLexer, fall through to pattern token
                                tokens.push(TokenStreamItem {
                                    index,
                                    token_type: rule.pattern.token,
                                    text: matched_text.to_string(),
                                });
                            }
                            _ => {
                                tokens.push(TokenStreamItem {
                                    index,
                                    token_type: rule.pattern.token,
                                    text: matched_text.to_string(),
                                });
                            }
                        }

                        // Handle state changes
                        if let LexerAction::Push(state) = &rule.action {
                            state_stack.push(state.clone());
                        } else if let LexerAction::PopN(n) = &rule.action {
                            for _ in 0..*n {
                                state_stack.pop();
                            }
                        } else if let LexerAction::TokenAndPush(_, state) = &rule.action {
                            state_stack.push(state.clone());
                        }
                        // LexerAction::Default is handled at construction time (generated code)
                        // LexerAction::Using falls through to pattern token in basic RegexLexer

                        index += 1;
                        pos = if end > pos { end } else { pos + 1 };
                        matched = true;
                        break;
                    }
                }
            }

            if !matched {
                // Check if current state has a default fallback
                let current_state = state_stack.last().unwrap();
                if let Some(target_state) = self.default_states.get(current_state) {
                    // Transition to default state
                    state_stack.push(target_state.clone());
                    continue;
                }

                // No rule matched at current position — consume one character as Text
                let ch_end = text[pos..].char_indices().nth(1)
                    .map(|(i, _)| pos + i)
                    .unwrap_or(text.len());
                tokens.push(TokenStreamItem {
                    index,
                    token_type: Token::TEXT,
                    text: text[pos..ch_end].to_string(),
                });
                index += 1;
                pos = ch_end;
            }
        }

        tokens
    }
}

impl Lexer for RegexLexer {
    fn name(&self) -> &str {
        &self.name
    }

    fn aliases(&self) -> &[&str] {
        &self.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.mimetypes
    }

    fn get_tokens_unprocessed(&self, text: &str) -> Vec<TokenStreamItem> {
        self.tokenize(text)
    }
}

/// A delegating lexer that wraps a root lexer and a language lexer.
/// The root lexer handles the template syntax, and the language lexer
/// handles embedded code blocks.
pub struct DelegatingLexer {
    pub name: String,
    pub aliases: Vec<String>,
    pub filenames: Vec<String>,
    pub mimetypes: Vec<String>,
    root: Box<dyn Lexer>,
    language: Box<dyn Lexer>,
}

impl DelegatingLexer {
    pub fn new(
        name: &str,
        root: Box<dyn Lexer>,
        language: Box<dyn Lexer>,
    ) -> Self {
        Self {
            name: name.to_string(),
            aliases: root.aliases().iter().map(|s| s.to_string()).collect(),
            filenames: root.filenames().iter().map(|s| s.to_string()).collect(),
            mimetypes: root.mimetypes().iter().map(|s| s.to_string()).collect(),
            root,
            language,
        }
    }
}

impl Lexer for DelegatingLexer {
    fn name(&self) -> &str {
        &self.name
    }

    fn aliases(&self) -> &[&str] {
        // Note: we store owned Strings but return &[&str] via temporary
        // This is a limitation — for now, return empty slice
        &[]
    }

    fn filenames(&self) -> &[&str] {
        &[]
    }

    fn mimetypes(&self) -> &[&str] {
        &[]
    }

    fn get_tokens(&self, code: &str) -> Vec<(crate::token::Token, String)> {
        self.root.get_tokens(code)
    }
}

/// Generate an optimized keyword regex using words().
pub fn words(keywords: &[&str], token: Token, prefix: &str, suffix: &str) -> LexerRule {
    let pattern_str = crate::regexopt::regex_opt(keywords, prefix, suffix);
    let pattern = TokenPattern::new(&pattern_str, token).unwrap();
    LexerRule {
        pattern,
        action: LexerAction::Token(token),
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_regex_lexer_basic() {
        let mut lexer = RegexLexer::new("test");
        lexer.aliases.push("test");

        // Add root state with keyword and identifier rules
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"(if|else|while)", Token::KEYWORD).unwrap(),
            action: LexerAction::token(Token::KEYWORD),
        });
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"[a-zA-Z_]\w*", Token::NAME).unwrap(),
            action: LexerAction::token(Token::NAME),
        });
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"\s+", Token::WHITESPACE).unwrap(),
            action: LexerAction::token(Token::WHITESPACE),
        });

        let tokens = lexer.tokenize("if x == 5");
        let token_types: Vec<Token> = tokens.iter().map(|t| t.token_type).collect();
        assert!(token_types.contains(&Token::KEYWORD));
        assert!(token_types.contains(&Token::NAME));
        assert!(token_types.contains(&Token::WHITESPACE));
    }

    #[test]
    fn test_multi_byte_char_fallback_at_end() {
        // Regression: multi-byte chars (emoji) at end of input that don't match
        // any rule must not panic via byte-boundary truncation.
        let mut lexer = RegexLexer::new("test");
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"\s+", Token::WHITESPACE).unwrap(),
            action: LexerAction::token(Token::WHITESPACE),
        });
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"[a-zA-Z_]\w*", Token::NAME).unwrap(),
            action: LexerAction::token(Token::NAME),
        });

        // Emoji alone — falls through to fallback path
        let tokens = lexer.tokenize("😊");
        assert_eq!(tokens.len(), 1);
        assert_eq!(tokens[0].text, "😊");
        assert_eq!(tokens[0].token_type, Token::TEXT);
    }

    #[test]
    fn test_multi_byte_char_fallback_after_identifier() {
        // Emoji after a matched identifier — fallback triggered at end
        let mut lexer = RegexLexer::new("test");
        lexer.add_rule("root", LexerRule {
            pattern: TokenPattern::new(r"[a-zA-Z_]\w*", Token::NAME).unwrap(),
            action: LexerAction::token(Token::NAME),
        });

        let tokens = lexer.tokenize("x😊");
        assert_eq!(tokens.len(), 2);
        assert_eq!(tokens[0].text, "x");
        assert_eq!(tokens[1].text, "😊");
    }
}
