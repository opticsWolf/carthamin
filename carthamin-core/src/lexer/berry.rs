// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: BerryLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated BerryLexer lexer.
pub struct BerryLexer {
    inner: RegexLexer,
}

impl BerryLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Berry");
        inner.aliases = vec![
            "berry",
            "be",
        ];
        inner.filenames = vec![
            "*.be",
        ];
        inner.mimetypes = vec![
            "text/x-berry",
            "application/x-berry",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: expr
        inner.states.insert("expr".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\.\.|[~!%^&*+=|?:<>/-]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"[(){}\[\],.;]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#-(.|\n)*?-#", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"#.*?$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
        ]);

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(as|break|continue|import|s(?:elf|tatic|uper))\b", Token::KEYWORD_RESERVED).unwrap(), action: LexerAction::token(Token::KEYWORD_RESERVED) },
            LexerRule { pattern: TokenPattern::new(r"(true|false|nil)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"(var|def)\b", Token::KEYWORD_DECLARATION).unwrap(), action: LexerAction::token(Token::KEYWORD_DECLARATION) },
        ]);

        // State: controls
        inner.states.insert("controls".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(break|continue|do|e(?:l(?:if|se)|nd|xcept)|for|if|r(?:aise|eturn)|try|while)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: builtins
        inner.states.insert("builtins".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(assert|b(?:ool|ytes)|c(?:all|lass(?:name|of)|ompile)|file|i(?:n(?:(?:(?:pu)?)t)|s(?:instance|subclass))|list|m(?:ap|odule)|number|open|print|r(?:ange|eal)|s(?:ize|(?:t|upe)r)|type)\b", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: numbers
        inner.states.insert("numbers".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"0[xX][a-fA-F0-9]+", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"-?\d+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
            LexerRule { pattern: TokenPattern::new(r"(-?\d+\.?|\.\d)\d*([eE][+-]?\d+)?", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) },
        ]);

        // State: name
        inner.states.insert("name".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b[^\W\d]\w*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
        ]);

        // State: funccall
        inner.states.insert("funccall".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b[^\W\d]\w*(?=\s*\()", Token::NAME_FUNCTION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: member
        inner.states.insert("member".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<=\.)\b[^\W\d]\w*\b(?!\()", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: strings
        inner.states.insert("strings".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##""([^\\]|\\.)*?""##, Token::STRING_DOUBLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\'([^\\]|\\.)*?\'", Token::STRING_SINGLE).unwrap(), action: LexerAction::PopN(1) },
        ]);

        BerryLexer { inner }
    }
}

impl Lexer for BerryLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
