// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: NumbaIRLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated NumbaIRLexer lexer.
pub struct NumbaIRLexer {
    inner: RegexLexer,
}

impl NumbaIRLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Numba_IR");
        inner.aliases = vec![
            "numba_ir",
            "numbair",
        ];
        inner.filenames = vec![
            "*.numba_ir",
        ];
        inner.mimetypes = vec![
            "text/x-numba_ir",
            "text/x-numbair",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"=", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\$[a-zA-Z0-9._]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"([a-zA-Z_]+[a-zA-Z0-9]*)", Token::NAME_CONSTANT).unwrap(), action: LexerAction::token(Token::NAME_CONSTANT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[0-9]+", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<[^>\n]*>", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[=<>{}\[\]()*.,!\':]|x\b", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });

        inner.states.insert("root".to_string(), root_rules);

        // State: keyword
        inner.states.insert("keyword".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(branch|call|del|jump) ", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        NumbaIRLexer { inner }
    }
}

impl Lexer for NumbaIRLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
