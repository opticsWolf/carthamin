// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: DarcsPatchLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated DarcsPatchLexer lexer.
pub struct DarcsPatchLexer {
    inner: RegexLexer,
}

impl DarcsPatchLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Darcs Patch");
        inner.aliases = vec![
            "dpatch",
        ];
        inner.filenames = vec![
            "*.dpatch",
            "*.darcspatch",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"<", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r">", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\{", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\}", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"New patches:", Token::GENERIC_HEADING).unwrap(), action: LexerAction::token(Token::GENERIC_HEADING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"Context:", Token::GENERIC_HEADING).unwrap(), action: LexerAction::token(Token::GENERIC_HEADING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"Patch bundle hash:", Token::GENERIC_HEADING).unwrap(), action: LexerAction::token(Token::GENERIC_HEADING) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\+", Token::GENERIC_INSERTED).unwrap(), action: LexerAction::Push("insert".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"-", Token::GENERIC_DELETED).unwrap(), action: LexerAction::Push("delete".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\]].*\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"\]", Token::OPERATOR).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: specialText
        inner.states.insert("specialText".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\[_[^_]*_]", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
        ]);

        // State: insert
        inner.states.insert("insert".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\[", Token::GENERIC_INSERTED).unwrap(), action: LexerAction::token(Token::GENERIC_INSERTED) },
            LexerRule { pattern: TokenPattern::new(r"[^\n\[]+", Token::GENERIC_INSERTED).unwrap(), action: LexerAction::token(Token::GENERIC_INSERTED) },
        ]);

        // State: delete
        inner.states.insert("delete".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\[", Token::GENERIC_DELETED).unwrap(), action: LexerAction::token(Token::GENERIC_DELETED) },
            LexerRule { pattern: TokenPattern::new(r"[^\n\[]+", Token::GENERIC_DELETED).unwrap(), action: LexerAction::token(Token::GENERIC_DELETED) },
        ]);

        DarcsPatchLexer { inner }
    }
}

impl Lexer for DarcsPatchLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
