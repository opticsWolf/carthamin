// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: MesonLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated MesonLexer lexer.
pub struct MesonLexer {
    inner: RegexLexer,
}

impl MesonLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Meson");
        inner.aliases = vec![
            "meson",
            "meson.build",
        ];
        inner.filenames = vec![
            "meson.build",
            "meson.options",
            "meson_options.txt",
        ];
        inner.mimetypes = vec![
            "text/x-meson",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#.*?$", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"'''.*'''", Token::STRING_SINGLE).unwrap(), action: LexerAction::token(Token::STRING_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[1-9][0-9]*", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"0o[0-7]+", Token::NUMBER_OCT).unwrap(), action: LexerAction::token(Token::NUMBER_OCT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"0x[a-fA-F0-9]+", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[a-zA-Z_][a-zA-Z_0-9]*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) });

        inner.states.insert("root".to_string(), root_rules);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[']{3}([']{0,2}([^\\']|\\(.|\n)))*[']{3}", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"'.*?(?<!\\)(\\\\)*?'", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: keywords
        inner.states.insert("keywords".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(break|continue|e(?:l(?:if|se)|nd(?:foreach|if))|foreach|if)\b", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: expr
        inner.states.insert("expr".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(in|and|or|not)\b", Token::OPERATOR_WORD).unwrap(), action: LexerAction::token(Token::OPERATOR_WORD) },
            LexerRule { pattern: TokenPattern::new(r"([\*/%\+-]=?|==|!=|=)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"[\[\]{}:().,?]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"((?:fals|tru)e)\b", Token::KEYWORD_CONSTANT).unwrap(), action: LexerAction::token(Token::KEYWORD_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"(build_machine|host_machine|meson|target_machine)\b", Token::NAME_VARIABLE_MAGIC).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE_MAGIC) },
        ]);

        // State: builtins
        inner.states.insert("builtins".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?<!\.)(a(?:dd_(?:global_(?:(?:(?:link_)?)arguments)|languages|project_(?:(?:(?:link_)?)arguments)|test_setup)|ssert)|b(?:enchmark|oth_libraries|uild_target)|c(?:onfigur(?:ation_data|e_file)|ustom_target)|d(?:e(?:(?:(?:clare_de)?)pendency)|isabler)|e(?:nvironment|rror|xecutable)|fi(?:les|nd_(?:library|program))|ge(?:nerator|t_(?:option|variable))|i(?:n(?:clude_directories|stall_(?:data|headers|man|subdir))|s_(?:disabler|variable))|j(?:ar|oin_paths)|library|message|project|r(?:ange|un_command)|s(?:et_variable|hared_(?:library|module)|tatic_library|u(?:b(?:dir(?:(?:_done)?)|project)|mmary))|test|(?:vcs_ta|warnin)g)\b", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
            LexerRule { pattern: TokenPattern::new(r"(?<!\.)import\b", Token::NAME_NAMESPACE).unwrap(), action: LexerAction::token(Token::NAME_NAMESPACE) },
        ]);

        MesonLexer { inner }
    }
}

impl Lexer for MesonLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
