// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: PropertiesLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated PropertiesLexer lexer.
pub struct PropertiesLexer {
    inner: RegexLexer,
}

impl PropertiesLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Properties");
        inner.aliases = vec![
            "properties",
            "jproperties",
        ];
        inner.filenames = vec![
            "*.properties",
        ];
        inner.mimetypes = vec![
            "text/x-java-properties",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[!#].*|/{2}.*", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });

        inner.states.insert("root".to_string(), root_rules);

        // State: key
        inner.states.insert("key".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\\:=\s]+", Token::NAME_ATTRIBUTE).unwrap(), action: LexerAction::token(Token::NAME_ATTRIBUTE) },
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\\\n]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: escapes
        inner.states.insert("escapes".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\\(.|\n)", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
        ]);

        PropertiesLexer { inner }
    }
}

impl Lexer for PropertiesLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
