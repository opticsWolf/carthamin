// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: SmaliLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated SmaliLexer lexer.
pub struct SmaliLexer {
    inner: RegexLexer,
}

impl SmaliLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Smali");
        inner.aliases = vec![
            "smali",
        ];
        inner.filenames = vec![
            "*.smali",
        ];
        inner.mimetypes = vec![
            "text/smali",
        ];

        // State: directive
        inner.states.insert("directive".to_string(), vec![
        ]);

        // State: access-modifier
        inner.states.insert("access-modifier".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(public|private|protected|static|final|synchronized|bridge|varargs|native|abstract|strictfp|synthetic|constructor|declared-synchronized|interface|enum|annotation|volatile|transient)", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        // State: instruction
        inner.states.insert("instruction".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\b[vp]\d+\b", Token::NAME_BUILTIN).unwrap(), action: LexerAction::token(Token::NAME_BUILTIN) },
        ]);

        // State: literal
        inner.states.insert("literal".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"".*""##, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"0x[0-9A-Fa-f]+t?", Token::NUMBER_HEX).unwrap(), action: LexerAction::token(Token::NUMBER_HEX) },
            LexerRule { pattern: TokenPattern::new(r"[0-9]*\.[0-9]+([eE][0-9]+)?[fd]?", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) },
            LexerRule { pattern: TokenPattern::new(r"[0-9]+L?", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
        ]);

        // State: field
        inner.states.insert("field".to_string(), vec![
        ]);

        // State: method
        inner.states.insert("method".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"<(?:cl)?init>", Token::NAME_FUNCTION).unwrap(), action: LexerAction::token(Token::NAME_FUNCTION) },
        ]);

        // State: label
        inner.states.insert("label".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r":\w+", Token::NAME_LABEL).unwrap(), action: LexerAction::token(Token::NAME_LABEL) },
        ]);

        // State: class
        inner.states.insert("class".to_string(), vec![
        ]);

        // State: punctuation
        inner.states.insert("punctuation".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"->", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"[{},():=.-]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
        ]);

        // State: type
        inner.states.insert("type".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[ZBSCIJFDV\[]+", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) },
        ]);

        // State: comment
        inner.states.insert("comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#.*?\n", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
        ]);

        SmaliLexer { inner }
    }
}

impl Lexer for SmaliLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
