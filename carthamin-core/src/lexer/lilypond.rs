// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: LilyPondLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated LilyPondLexer lexer.
pub struct LilyPondLexer {
    inner: RegexLexer,
}

impl LilyPondLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("LilyPond");
        inner.aliases = vec![
            "lilypond",
        ];
        inner.filenames = vec![
            "*.ly",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"%\{.*?%\}", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"%.*?$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[#$]@?", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("value".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?x)
               \\\\
               | (?<= \s ) (?: -- | __ )
               | [{}<>=.,:|]
              ", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r##"[\-_^]?""##, Token::STRING).unwrap(), action: LexerAction::Push("string".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"-?\d+\.\d+", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?x)
               (?<= \s ) -\d+
               | (?: (?: \d+ | \\breve | \\longa | \\maxima )
                     \.* )
              ", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\*", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[\-_^]?\\(a(?:(?:ccept|ddlyric|lia)s)|book(?:(?:part)?)|c(?:hord(?:mode|s)|on(?:sists|text))|d(?:e(?:faultchild|nies|scription)|rum(?:mode|s))|etc|figure(?:mode|s)|header|in(?:clude|herit\-acceptability)|l(?:a(?:nguage|yout)|yric(?:mode|sto))|midi|n(?:ame|ew|otemode)|paper|remove|score|type|version|with)(?=\d|[^\w\-]|[\-_][\W\d])", Token::KEYWORD).unwrap(), action: LexerAction::token(Token::KEYWORD) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[\-_^]?\\(cm|in|mm|pt|staff\-space)(?=\d|[^\w\-]|[\-_][\W\d])", Token::NUMBER).unwrap(), action: LexerAction::token(Token::NUMBER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"([^\W\d]|-)+?(?=\d|[^\w\-]|[\-_][\W\d])", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r".", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });

        inner.states.insert("root".to_string(), root_rules);

        // State: string
        inner.states.insert("string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\\.", Token::STRING_ESCAPE).unwrap(), action: LexerAction::token(Token::STRING_ESCAPE) },
            LexerRule { pattern: TokenPattern::new(r#"[^\\"]+"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
        ]);

        // State: value
        inner.states.insert("value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#\{", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r";.*?$", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"#\|", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("multiline-comment".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"#;[([]", Token::COMMENT).unwrap(), action: LexerAction::Push("commented-form".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"#;", Token::COMMENT).unwrap(), action: LexerAction::Push("commented-datum".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"#!r6rs", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
            LexerRule { pattern: TokenPattern::new(r"(?x)
          (
            
          (
            ( \#[bB] ) (\#[iIeE])?
            | \#[iIeE] ( \#[bB] )
          )
        
            
          (
            
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [01]+
            ( / [01]+ )?
          )
            # Sign optional
          )
        ?  [+-]  ((nan.0|inf.0)|
          (
            [01]+
            ( / [01]+ )?
          )
        )?  i
            | 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [01]+
            ( / [01]+ )?
          )
            # Sign optional
          )
         (@ 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [01]+
            ( / [01]+ )?
          )
            # Sign optional
          )
        )?

          )
        
          )
          # Need to ensure we have a full token. 1+ is not a
          # number followed by something else, but a function
          # name.
          
      (?=
        \s         # whitespace
        | ;        # comment
        | \#[;|!] # fancy comments
        | [)\]]    # end delimiters
        | $        # end of file
      )
    
        ", Token::NUMBER_BIN).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"(?x)
          (
            
          (
            ( \#[oO] ) (\#[iIeE])?
            | \#[iIeE] ( \#[oO] )
          )
        
            
          (
            
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-7]+
            ( / [0-7]+ )?
          )
            # Sign optional
          )
        ?  [+-]  ((nan.0|inf.0)|
          (
            [0-7]+
            ( / [0-7]+ )?
          )
        )?  i
            | 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-7]+
            ( / [0-7]+ )?
          )
            # Sign optional
          )
         (@ 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-7]+
            ( / [0-7]+ )?
          )
            # Sign optional
          )
        )?

          )
        
          )
          # Need to ensure we have a full token. 1+ is not a
          # number followed by something else, but a function
          # name.
          
      (?=
        \s         # whitespace
        | ;        # comment
        | \#[;|!] # fancy comments
        | [)\]]    # end delimiters
        | $        # end of file
      )
    
        ", Token::NUMBER_OCT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"(?x)
          (
            
          (
            ( \#[xX] ) (\#[iIeE])?
            | \#[iIeE] ( \#[xX] )
          )
        
            
          (
            
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-9a-fA-F]+
            ( / [0-9a-fA-F]+ )?
          )
            # Sign optional
          )
        ?  [+-]  ((nan.0|inf.0)|
          (
            [0-9a-fA-F]+
            ( / [0-9a-fA-F]+ )?
          )
        )?  i
            | 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-9a-fA-F]+
            ( / [0-9a-fA-F]+ )?
          )
            # Sign optional
          )
         (@ 
          (
            [+-] (nan.0|inf.0)  # Sign mandatory
            | [+-]? 
          (
            [0-9a-fA-F]+
            ( / [0-9a-fA-F]+ )?
          )
            # Sign optional
          )
        )?

          )
        
          )
          # Need to ensure we have a full token. 1+ is not a
          # number followed by something else, but a function
          # name.
          
      (?=
        \s         # whitespace
        | ;        # comment
        | \#[;|!] # fancy comments
        | [)\]]    # end delimiters
        | $        # end of file
      )
    
        ", Token::NUMBER_HEX).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::Push("string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"'[\w!$%&*+,/:<=>?@^~|-]+", Token::STRING_SYMBOL).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"#:[\w!$%&*+,/:<=>?@^~|-]+", Token::KEYWORD_DECLARATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r#"#\\([()/'\"._!§$%& ?=+-]|[a-zA-Z0-9]+)"#, Token::STRING_CHAR).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"(#t|#f)", Token::NAME_CONSTANT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"('|#|`|,@|,|\.)", Token::OPERATOR).unwrap(), action: LexerAction::token(Token::OPERATOR) },
            LexerRule { pattern: TokenPattern::new(r"(?<='\()[\w!$%&*+,/:<=>?@^~|-]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"(?<=#\()[\w!$%&*+,/:<=>?@^~|-]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"(?<=\()[\w!$%&*+,/:<=>?@^~|-]+", Token::NAME_FUNCTION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[\w!$%&*+,/:<=>?@^~|-]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[([]", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("scheme-root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[)\]]", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: maybe-subproperties
        inner.states.insert("maybe-subproperties".to_string(), vec![
        ]);

        // State: multiline-comment
        inner.states.insert("multiline-comment".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#\|", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::Push("root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\|#", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^|#]+", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
            LexerRule { pattern: TokenPattern::new(r"[|#]", Token::COMMENT_MULTILINE).unwrap(), action: LexerAction::token(Token::COMMENT_MULTILINE) },
        ]);

        // State: commented-form
        inner.states.insert("commented-form".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[([]", Token::COMMENT).unwrap(), action: LexerAction::Push("root".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[)\]]", Token::COMMENT).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^()[\]]+", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) },
        ]);

        // State: commented-datum
        inner.states.insert("commented-datum".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"(?x).*?
      (?=
        \s         # whitespace
        | ;        # comment
        | \#[;|!] # fancy comments
        | [)\]]    # end delimiters
        | $        # end of file
      )
    ", Token::COMMENT).unwrap(), action: LexerAction::PopN(1) },
        ]);

        LilyPondLexer { inner }
    }
}

impl Lexer for LilyPondLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
