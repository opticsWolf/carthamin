// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: NginxConfLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated NginxConfLexer lexer.
pub struct NginxConfLexer {
    inner: RegexLexer,
}

impl NginxConfLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Nginx configuration file");
        inner.aliases = vec![
            "nginx",
        ];
        inner.filenames = vec![
            "nginx.conf",
        ];
        inner.mimetypes = vec![
            "text/x-nginx-conf",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[^\s;#]+", Token::KEYWORD).unwrap(), action: LexerAction::Push("stmt".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: block
        inner.states.insert("block".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"[^\s;#]+", Token::KEYWORD_NAMESPACE).unwrap(), action: LexerAction::Push("stmt".to_string()) },
        ]);

        // State: stmt
        inner.states.insert("stmt".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("block".to_string()) },
            LexerRule { pattern: TokenPattern::new(r";", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: base
        inner.states.insert("base".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"#.*\n", Token::COMMENT_SINGLE).unwrap(), action: LexerAction::token(Token::COMMENT_SINGLE) },
            LexerRule { pattern: TokenPattern::new(r"on|off", Token::NAME_CONSTANT).unwrap(), action: LexerAction::token(Token::NAME_CONSTANT) },
            LexerRule { pattern: TokenPattern::new(r"\$[^\s;#()]+", Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) },
            LexerRule { pattern: TokenPattern::new(r"[a-z-]+/[a-z-+]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"[0-9]+[km]?\b", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) },
            LexerRule { pattern: TokenPattern::new(r"[:=~]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"[^\s;#{}$]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"/[^\s;#]*", Token::NAME).unwrap(), action: LexerAction::token(Token::NAME) },
            LexerRule { pattern: TokenPattern::new(r"[$;]", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
        ]);

        NginxConfLexer { inner }
    }
}

impl Lexer for NginxConfLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
