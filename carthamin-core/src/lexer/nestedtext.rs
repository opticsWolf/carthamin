// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: NestedTextLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated NestedTextLexer lexer.
pub struct NestedTextLexer {
    inner: RegexLexer,
}

impl NestedTextLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("NestedText");
        inner.aliases = vec![
            "nestedtext",
            "nt",
        ];
        inner.filenames = vec![
            "*.nt",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();


        inner.states.insert("root".to_string(), root_rules);

        // State: inline_list
        inner.states.insert("inline_list".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\{\}\[\],\s]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) },
            LexerRule { pattern: TokenPattern::new(r"\]", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::ERROR).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: inline_dict
        inner.states.insert("inline_dict".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\{\}\[\],:\s]+", Token::NAME_TAG).unwrap(), action: LexerAction::token(Token::NAME_TAG) },
            LexerRule { pattern: TokenPattern::new(r":", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("inline_dict_value".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\n", Token::ERROR).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: inline_dict_value
        inner.states.insert("inline_dict_value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"[^\{\}\[\],:\s]+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) },
            LexerRule { pattern: TokenPattern::new(r",", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
            LexerRule { pattern: TokenPattern::new(r"\}", Token::PUNCTUATION).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: inline_value
        inner.states.insert("inline_value".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"\{", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("inline_dict".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"\[", Token::PUNCTUATION).unwrap(), action: LexerAction::Push("inline_list".to_string()) },
        ]);

        // State: whitespace
        inner.states.insert("whitespace".to_string(), vec![
        ]);

        NestedTextLexer { inner }
    }
}

impl Lexer for NestedTextLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
