// AUTO-GENERATED by generators/gen_lexers.py — DO NOT EDIT BY HAND
// Lexer: TurtleLexer

use crate::token::Token;
use crate::lexer::{Lexer, RegexLexer, LexerRule, LexerAction};
use crate::scanner::TokenPattern;

/// Auto-generated TurtleLexer lexer.
pub struct TurtleLexer {
    inner: RegexLexer,
}

impl TurtleLexer {
    pub fn new() -> Self {
        let mut inner = RegexLexer::new("Turtle");
        inner.aliases = vec![
            "turtle",
        ];
        inner.filenames = vec![
            "*.ttl",
        ];
        inner.mimetypes = vec![
            "text/turtle",
            "application/x-turtle",
        ];

        // Root state
        let mut root_rules: Vec<LexerRule> = Vec::new();

            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\s+", Token::TEXT).unwrap(), action: LexerAction::token(Token::TEXT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"(?<=\s)a(?=\s)", Token::KEYWORD_TYPE).unwrap(), action: LexerAction::token(Token::KEYWORD_TYPE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r#"(<[^<>"{}|^`\\\x00-\x20]*>)"#, Token::NAME_VARIABLE).unwrap(), action: LexerAction::token(Token::NAME_VARIABLE) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"#([^\n]+|$)", Token::COMMENT).unwrap(), action: LexerAction::token(Token::COMMENT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"\b(true|false)\b", Token::LITERAL).unwrap(), action: LexerAction::token(Token::LITERAL) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[+\-]?\d*\.\d+", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[+\-]?\d*(:?\.\d+)?E[+\-]?\d+", Token::NUMBER_FLOAT).unwrap(), action: LexerAction::token(Token::NUMBER_FLOAT) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[+\-]?\d+", Token::NUMBER_INTEGER).unwrap(), action: LexerAction::token(Token::NUMBER_INTEGER) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"[\[\](){}.;,:^]", Token::PUNCTUATION).unwrap(), action: LexerAction::token(Token::PUNCTUATION) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r##"""""##, Token::STRING).unwrap(), action: LexerAction::Push("triple-double-quoted-string".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::Push("single-double-quoted-string".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"'''", Token::STRING).unwrap(), action: LexerAction::Push("triple-single-quoted-string".to_string()) });
            root_rules.push(LexerRule { pattern: TokenPattern::new(r"'", Token::STRING).unwrap(), action: LexerAction::Push("single-single-quoted-string".to_string()) });

        inner.states.insert("root".to_string(), root_rules);

        // State: triple-double-quoted-string
        inner.states.insert("triple-double-quoted-string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"""""##, Token::STRING).unwrap(), action: LexerAction::Push("end-of-string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r#"[^\\]+(?=""")"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\\", Token::STRING).unwrap(), action: LexerAction::Push("string-escape".to_string()) },
        ]);

        // State: single-double-quoted-string
        inner.states.insert("single-double-quoted-string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r##"""##, Token::STRING).unwrap(), action: LexerAction::Push("end-of-string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r#"[^"\\\n]+"#, Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\\", Token::STRING).unwrap(), action: LexerAction::Push("string-escape".to_string()) },
        ]);

        // State: triple-single-quoted-string
        inner.states.insert("triple-single-quoted-string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"'''", Token::STRING).unwrap(), action: LexerAction::Push("end-of-string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[^\\]+(?=''')", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\\", Token::STRING).unwrap(), action: LexerAction::Push("string-escape".to_string()) },
        ]);

        // State: single-single-quoted-string
        inner.states.insert("single-single-quoted-string".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r"'", Token::STRING).unwrap(), action: LexerAction::Push("end-of-string".to_string()) },
            LexerRule { pattern: TokenPattern::new(r"[^'\\\n]+", Token::STRING).unwrap(), action: LexerAction::token(Token::STRING) },
            LexerRule { pattern: TokenPattern::new(r"\\", Token::STRING).unwrap(), action: LexerAction::Push("string-escape".to_string()) },
        ]);

        // State: string-escape
        inner.states.insert("string-escape".to_string(), vec![
            LexerRule { pattern: TokenPattern::new(r".", Token::STRING).unwrap(), action: LexerAction::PopN(1) },
        ]);

        // State: end-of-string
        inner.states.insert("end-of-string".to_string(), vec![
        ]);

        TurtleLexer { inner }
    }
}

impl Lexer for TurtleLexer {
    fn get_tokens(&self, code: &str) -> Vec<(Token, String)> {
        self.inner.get_tokens(code)
    }

    fn name(&self) -> &str {
        &self.inner.name
    }

    fn aliases(&self) -> &[&str] {
        &self.inner.aliases
    }

    fn filenames(&self) -> &[&str] {
        &self.inner.filenames
    }

    fn mimetypes(&self) -> &[&str] {
        &self.inner.mimetypes
    }
}
